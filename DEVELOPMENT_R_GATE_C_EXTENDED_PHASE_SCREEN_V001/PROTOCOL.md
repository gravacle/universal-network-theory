# Frozen Gate R-C rho=1/4 centerline scaling screen

## Scope and claim boundary

The preceding hostile-audited seed screen found no isolated critical-density
candidate through `L=8`.  This packet is Checkpoint 1 of the replacement
algebraic-scaling route.  It tests only whether the response-active centerline
at sharp sector density `rho=1/4` remains compatible with dynamic exponent
`z=1` through `L=12`.  It does not yet test a density interval, reopen the
isolated `rho_c` search, or define a thermodynamic phase, continuum, metric,
Ward law, graviton, gravity, or macroscopic emergence.

The physical parent remains the owner-once degree-three periodic prism

```text
H_L = - sum_(e in E_L) T_e,       Q = sum_v n_v,       [H_L,Q] = 0.
```

`H_L` has no accumulated-write-count argument.  Sharp sector density is
`rho=q/(2L)`.  Sector projection remains an adopted analytical diagnostic,
not an authenticated post-selected accumulation history.

## Frozen centerline rows

Use the single exact-density centerline

```text
rho_* = 1/4,       L in {4,6,8,10,12},       q=L/2.
```

This predeclares exactly two new target rows

```text
L10: q=5
L12: q=6.
```

The sealed complete-spectrum `L4 q2`, `L6 q3`, and `L8 q4` rows are inputs.
No interpolation or off-centerline sector is authorized in this checkpoint.

## Response channel and Krylov solver

Use the same total-rail `m=1` density mode and definitions sealed in
`DEVELOPMENT_R_GATE_C_HYDRO_CRITICALITY_PRESCREEN_V001/PROTOCOL.md`:

```text
Delta_act = lowest positive pole with authenticated numerical response weight
chi_tau   = sqrt[sum_a w_a/omega_a^2 / sum_a w_a]
R_low     = w_(Delta_act)/sum_a w_a.
```

Resolve the translation-zero Perron ground state and the response momentum
`m=L-1` in independently constructed translation blocks.  Dense conversion is
forbidden.  The target uses a sparse owner-action and fully reorthogonalized
Lanczos/Rayleigh--Ritz sequence at dimensions `16,32,64,96,128`.  It targets
the response cyclic subspace generated by `n_k|0>`, rather than an arbitrary
fixed number of algebraically lowest eigenvalues.

A row is resolved only if all of the following hold:

1. ground and promoted-pole Ritz residuals are at most `1e-9`;
2. block Hermiticity error is at most `1e-12`;
3. Lanczos orthogonality error is at most `1e-10`;
4. `Delta_act`, `chi_tau`, and `R_low` agree between the last two available
   Krylov checkpoints to relative `2e-7`, `2e-7`, and `2e-6` respectively;
5. the pole remains active at `w_floor/10`, `w_floor`, and `10*w_floor`, with
   `w_floor=max(1e-12 sum w,100 r_max^2 ||n_k|0>||^2)`; and
6. `R_low >= 1e-6`.

If 128 vectors do not resolve a row, classify the screen `UNRESOLVED`; do not
increase the basis or substitute a dense calculation.

`chi_tau` may also be reconstructed by Krylov conjugate-gradient solution of
`(H-E0)x=n_k|0>` as an independent check.  It remains a finite
response-timescale statistic, not a thermodynamic susceptibility.

## Frozen z=1 finite-size classification

Form the five exact-density rows and define

```text
X_Delta(L)=L Delta_act(L,rho_*),
X_chi(L)=chi_tau(L,rho_*)/L.
```

Fit `Delta_act` to three predeclared families

```text
fixed-z1:      a/L + b/L^2,
free-gapless:  a L^(-z),
positive-gap:  Delta_inf + a L^(-z),
```

with `a>0`, `Delta_inf>0`, and `1/4<=z<=4`.  The correction coefficient `b`
may have either sign, but the fitted fixed-z1 gap must remain positive at all
five sizes.  Select parameters by least squares on four sizes and score
squared relative prediction error on the held-out size; sum all five held-out
errors.  Fit `chi_tau=c L^y` on the log scale and separately track `chi_tau/L`.

A finite-window `z=1` centerline candidate passes only if:

1. `Delta_act` strictly decreases and `chi_tau` strictly increases with L;
2. the fixed-z1 family has lower total held-out relative error than the
   positive-gap family and no more than `1.25` times the free-gapless error;
3. fitted `z` and `y` both lie in `[0.90,1.10]` and `|z-y|<=0.10`;
4. over `L={8,10,12}`, the relative ranges of both `X_Delta` and `X_chi` are
   at most `0.05`; and
5. the aligned lowest-pole residue stays above `1e-6` and its relative range
   over `L={8,10,12}` is at most `0.35`.

Return `CENTERLINE_Z1_COMPATIBLE_L4_L12` only if every check passes.  Otherwise
return `CENTERLINE_Z1_REJECTED_L4_L12`, or `UNRESOLVED` if a required row is
numerically unresolved.  Rejection halts only this proposed algebraic-scaling
route.  Compatibility authorizes a separately frozen native-operator algebra
screen; it does not prove exact `z=1` or a thermodynamic phase.

## Independent hostile requirement

Before target L10/L12 output is inspected, freeze a second implementation
that reconstructs translation orbits, block action, Krylov sequences,
classification without importing the target module or its raw matrices.  It
must compare every raw required row and every fitted
decision.  Numerical agreement limits are `2e-8` relative for energies and
gaps, `5e-7` for `chi_tau`, and `5e-6` for residues.  A mismatch fails closed.

## Resource and stop guards

- Each process runs one sector.  After target and blind implementations are
  hash-frozen, target/blind `L10 q5` and `L12 q6` workers may run concurrently
  as four one-thread processes with disjoint outputs.
- Maximum 128 retained Krylov vectors per solve.
- Abort a process above 6 GiB RSS, abort the wave above 32 GiB aggregate RSS,
  and abort a sector above 2,000 total matrix-vector products or
  three wall hours.
- Preserve basis/block dimensions, nonzero counts, matvec counts, residuals,
  convergence sequences, wall time, and peak RSS.
- Do not initiate native-algebra, macroscopic-closure, universality,
  spatial-response, or two-cluster work until this centerline result passes an
  independent hostile audit.
- If the result is hostile-audited compatible, only the native-operator
  algebra screen is next.  No spacetime algebra is inserted as a premise.

**Proved inputs:** finite owner topology, number conservation, translation
symmetry, one-carrier bands, and the previously sealed L4/L6/L8 records.

**Adopted:** sharp-sector diagnostic, total-rail `m=1` channel, the `rho=1/4`
centerline, solver tolerances, scaling families, and pass thresholds.

**Empirical:** all L10/L12 Ritz data, exponents, held-out errors, and the
finite-window centerline classification.

**Open:** a thermodynamic phase or critical interval, authenticated
accumulation selection of those sectors, macroscopic closure, universal
coupling, continuum behavior, attraction, emergence, and gravity.
