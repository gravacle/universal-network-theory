# Universal Network Theory architecture

Version: V001  
Date: 2026-09-16  
Status: `CANONICAL_TERMINOLOGY__ARGER_GATE_ADOPTED__PROOF_LEVELS_EXPLICIT`

## 1. Governing framework

**Universal Network Theory (UNT)** is the governing framework. It is executed
through two distinct but joined mechanisms:

1. **Record Formation Theory (RFT)** is the microscopic construction. It
   characterizes discrete causal record structures, their admissible
   transitions, retained ancestry, and finite observability. Its exact Axiomatic
   Universal Record Formation Theory (AURFT) theorem proves Coverage-U for
   independently fixed actual records inside the adopted Universal
   Directed-Composition Law (U-DCL); the parent physical dynamics supplies the
   admissible actual record to which that theorem applies.
2. **Gravity Formation Theory (GFT)** is UNT's gravity-formation and response
   mechanism. Its finite formation layer asks when a qualified discrete record
   structure satisfies the adopted project-native finite ARGER Gate. Its
   separately typed response layer states the conditional macroscopic
   gravitational response under the adopted Record--Geometry Realization Law
   (RGRL) and working-theory-closure hypotheses `WTC-H1` through `WTC-H5`.
   Passing the finite Gate does not establish those additional premises.

In the finite relational family, `L` is the even size and one-pass horizon of
an independently constructed owner-once periodic-prism domain: `L` active
cells, `2L` carrier sites, `3L` transport edges, and `L` fresh admissions
before revisit. It is distinct from event-prefix depth `n` and sector rank
`q`. The authenticated `L = 4` through `L = 12` result is a finite small-system
theorem and computation; it is not itself a macroscopic construction. Its
macroscopic significance is that it supplies GFT's authenticated microscopic
classification. The conditional response theorem is a separate result under
its own explicit premises. GFT contains both layers without turning either
one into proof of the other's premises.

The **Universal Record Model (URM)** is the executable integration and custody
surface for UNT. It is not a third theory and does not create scientific
authority. It exposes versioned definitions, theorem certificates, evidence
hashes, status ceilings, and refusal conditions for RFT and GFT results.

The controlling composition theorem is
[`UNT-CLOSURE-V001`](UNIVERSAL_NETWORK_THEORY_CLOSURE_THEOREM_V001.md). This
architecture document controls terminology and layer ownership; the closure
theorem controls the exact type-preserving composition of the completed RFT
and GFT results.

## 2. The ARGER record-formation sequence

ARGER is UNT's central ordered record-formation dependency sequence:

```text
ALLOW -> REQUIRE -> GATE -> EM -> RECORD
   A         R         G       E       R
```

The repeated `R` is intentional: **REQUIRE** is the physical narrowing step;
**RECORD** is the retained causal object produced by the pipeline.

| stage | role | proof-safe meaning |
|:---|:---|:---|
| `ALLOW` | topological capacity | identifies the admissible finite record configurations and continuation actions without selecting one realized physical sector |
| `REQUIRE` | physical narrowing | imposes the independently warranted conditions needed for the realized sector or observable; it must not be inferred from generic recordhood alone |
| `GATE` | phase classifier | applies one declared, versioned physical criterion to the complete mathematical object that the criterion purports to classify |
| `EM` | physical binding/readout | supplies the declared finite observable and, when a governing compact-`U(1)` domain has independently been established, its domain-matched coupling data |
| `RECORD` | retained causal ancestry | is the lineage-bearing, auditable causal structure on which the previous stages act |

ARGER organizes the record-formation roles and their dependency order; that
organization does not license moving evidence between types. In particular,
`ALLOW` does not imply `REQUIRE`; positive visibility does not by itself select
a numerical coupling; and the URM does not turn a Gate declaration into a
proved physical law. The parent physical dynamics supplies the admissible actual
record to which AURFT's coverage theorem applies.

In the present finite application, exact bounded membership and majority mass
establish the complete record block as the mathematical evaluation domain.
Sector-complete positive finite visibility supplies the independently checked
physical narrowing/readout condition. The Gate acts on the complete domain,
not on isolated atoms, while the retained ancestry remains explicit in the
membership theorem.

## 3. The `L = 4` through `L = 12` record-envelope result

The current exact RFT theorem is the
[`L4--L12 extendible record-envelope theorem`](L4_L12_EXTENDIBLE_RECORD_BLOCK_THEOREM_2026-09-16.md),
independently checked by its
[`hostile audit`](AUDIT_L4_L12_EXTENDIBLE_RECORD_BLOCK_THEOREM_2026-09-16.md).
For each authenticated size `L in {4,6,8,10,12}`, the fixed density envelope
`A009--A016` selects a bounded rank block whose realized probability-support
components have their exact role:

- a lower endpoint has actual lower-rank ancestry and a non-dark admissible
  continuation;
- an intermediate selected rank has actual ancestry through the lower endpoint and a
  non-dark admissible continuation; and
- the upper endpoint has actual ancestry through all lower selected ranks and
  completes the bounded block.

The theorem deliberately does not require further continuation from the upper
endpoint. Indeed, it proves that every upper sector contains dark terminal
basis states. That sharp endpoint is part of the result, not a defect to be
hidden.

After deduplicating sectors, the authenticated block masses are:

| L | selected ranks | block mass |
|---:|:---|---:|
| 4 | `1--2` | `0.7260206189754993` |
| 6 | `2--3` | `0.5846615608350367` |
| 8 | `2--4` | `0.7373965730354166` |
| 10 | `3--5` | `0.6500987927669427` |
| 12 | `4--6` | `0.56956498393327842` |

Every value is greater than `0.50`. At `L = 12` the majority margin is

```text
0.56956498393327842 - 0.50 = 0.06956498393327842.
```

That difference is presently an exact arithmetic margin. It is **not yet
proved** to be an alpha-regulated write cost, binding energy, mass defect, or
other independently defined physical quantity.

## 4. Finite spectral visibility

The frozen spectral calculations provide a separate finite fact about the
same `A009--A016` envelope. The 40 atom/size assignments reduce to 13 unique
selected `(L,q)` sectors. Every one has strictly positive low-pole residue,
with independent Target and Blind realizations for `L = 10` and `L = 12`.
These 13 `(L,q)` rows span five independently constructed finite domains; they
are not 13 sectors within one `L = 12` system. Across all 13 rows,

```text
R_low >= 0.4280947078156539 > 0
```

and the independent implementations agree within their authenticated
numerical tolerance. At `L = 12` the selected q4/q5/q6 Target residues are
respectively

```text
0.8417832705849726,
0.8311603546876659,
0.8251471862764429.
```

This proves finite probe visibility for every constituent sector of the
record envelope and supplies the visibility input to the adopted finite ARGER
Gate. It does not by itself prove an asymptotic dynamical exponent.

### 4.1 Separately typed joint lineage--carrier evidence

The owner-once engine also carries a different exact observable: a centered
within-sector covariance between the retained lineage register and carrier
occupancy after the first-pass schedule. Independent Target and Hostile
implementations reproduce

```text
D_10 = -0.00031184885937284
D_12 = -0.00003236588485224
T_10:12 = 32365.88485223031.
```

This is finite evidence of nonzero **two-sided** lineage--carrier association
through `L = 12`. It is not the strict common-lineage-support diagnostic,
record-block mass, or finite spectral visibility, and it is not an ARGER Gate
premise.

The stronger signed positive no-decline criterion froze
`f = D_8 = 0.001963064475535806` before unblinding and fails: the held-out
witness reverses sign and decreases in magnitude. Therefore the result may not
be described as positive scale-stable persistence or an all-`L` law.

A `10,748 s` branch-release skew exceeded a legacy `60 s`
anti-contamination rule. Current documentation discloses that operational
deviation and withholds the narrow pristine strict-protocol label. It does not
discard the deterministic independent agreement: neither final value existed
at launch, and cross-branch value access remained closed until Target
completion. The deviation and the scientific result are typed separately.

Nothing in this witness establishes conditional dynamical `z=1`, RGRL/WTC,
alpha, record curvature, spacetime geometry, Einstein response, or gravity.

## 5. The adopted finite ARGER Gate

The project adoption decision is recorded in
[`ARGER_GATE_ADOPTION_2026-09-16.md`](ARGER_GATE_ADOPTION_2026-09-16.md).
The governing native UNT criterion is:

> **Finite ARGER Gate.** An authenticated finite record
> envelope passes the finite ARGER Gate when (i) exact bounded record
> membership has been proved for every selected sector, (ii) its deduplicated
> authenticated probability mass exceeds one half, and (iii) every unique
> selected sector has strictly positive authenticated finite probe visibility.
> A passing envelope realizes the UNT finite GFT `z=1` standard.

The record block is the evidence domain evaluated at ARGER's single `GATE`
step; it is not part of ARGER's identity. ARGER remains the complete `ALLOW ->
REQUIRE -> GATE -> EM -> RECORD` process. The ARGER Gate is adopted as the
governing finite Gate, not left as a proposal. Numbered labels preserved inside
sealed 2026-09-16 evidence are archival implementation identifiers for this
same Gate, not a sequence of different gates.
The `L = 4` through `L = 12` membership theorem and its hostile audit, the independently
reconstructed majority masses, and the 13 authenticated positive-visibility
rows therefore establish the finite GFT `z=1` classification at each of the
five authenticated sizes, including `L = 12`. The global minimum visibility is
`R_low = 0.4280947078156539`.

Two `z=1` statements remain deliberately separate:

- **Finite GFT `z=1`.** This is the adopted UNT Gate classification proved
  by the complete authenticated construction through `L = 12`. `L = 12`
  closes the evidence obligation for this Gate application.
- **Conditional dynamical `z=1`.** The separate same-model LL-P theorem
  supports dynamical exponent `z=1` on the declared density interval. LL-P is
  independently typed and is supplementary to, rather than an unfinished
  premise of, the finite ARGER Gate.

## 6. Alpha in ARGER

For alpha's role inside Record Formation, alpha is constitutive identity data
of a governing domain.

- `ALLOW`: for the declared finite active-EM construction the exact admitted
  set is
  `I_chi intersect ((-ln(1-delta))/(4*pi*B^2), infinity)`; at zero relevance
  floor it is every positive alpha in `I_chi`. The two exact fixed-control
  witnesses bracket the observed alpha.
- `REQUIRE`: conditional on independently established ancestry to a governing
  compact-`U(1)` domain satisfying `SAI1--SAI8`, every coefficient-preserving
  same-sector record inherits that domain's measured alpha and RG trajectory.
  The measured alpha is that domain's aligned base coupling; an inequivalent
  aligned alpha cannot be assigned independently to an SAI-scoped same-domain
  record.
- `SELECT`: governing-domain selection is a separately typed theorem class.

The interval is scoped to the declared ideal finite-record construction. Its
`0.50` cavity-read witness and the Gravity Formation majority Gate are distinct
typed quantities on distinct proof surfaces.

The `L = 12` majority margin `0.06956498393327842` is exact Gate arithmetic.
Any proposed relation to alpha write cost, probability-flow amplitude, or
spectral residue is a separately dimension-checked hypothesis requiring its
own theorem and evidence on the alpha proof surface.

## 7. L14 boundary

The 2026-09-15/16 L14 Target/Hostile run ended without completed branch
results. Its disposition is
[`INCOMPLETE_PRESERVED_NO_SCIENTIFIC_RESULT`](history/L14_RUN_DISPOSITION_2026-09-16.md).
It supplies no L14 value and neither proves nor refutes the ARGER Gate result.

L14 is unnecessary for the exact `L = 4` through `L = 12` structural theorem or the adopted
finite ARGER Gate. It would remain useful as a future finite-size stress test,
but it is not silently inserted as a premise of the `L = 12` block result.

## 8. Canonical claim language

Supported now:

> The program proves and independently audits an exact bounded extendible
> record envelope at `L = 4` through `L = 12`. Its deduplicated authenticated mass is a majority
> at every tested size and is `0.56956498393327842` at `L = 12`. Every constituent
> of that envelope has strictly positive authenticated finite probe visibility.
> Under the adopted UNT finite ARGER Gate, those facts establish the finite
> block `z=1` classification at every authenticated size from `L = 4` through `L = 12`.

Supported under the separately typed same-model physical premise:

> Under LL-P, the identified hard-core-boson ladder has dynamical exponent
> `z=1` throughout the declared density interval. This conditional dynamical
> theorem supports, but is not required by, the finite ARGER Gate.

Not supported without further work:

- a parameter-free numerical derivation of `G`;
- a numerical derivation or selection of alpha; or
- identification of the `0.06956498393327842` majority margin with an alpha
  write cost or binding energy.
