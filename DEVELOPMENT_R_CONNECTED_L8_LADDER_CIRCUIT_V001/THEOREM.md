# Connected L8 paired-ring circuit — numerical result

## Selected finite parent

In the prepared `V_8=(Z/8Z)^3` family, retained heads have labels
`(2r+1,y,z)`. For each fixed odd first label and even `z`, take the two native
eight-record cycles obtained by varying `y` at `z` and `z+1`, and include the
eight native third-generator supports pairing equal `y`. This partitions all
256 heads into 16 connected, 16-record support graphs.

These tuple labels and generator incidences are inherited finite-record
enumeration. They are not promoted to a physical grid, distance, continuum,
or graviton. Every one of the 24 native supports is applied exactly once per
circuit depth step. The explicit gate order is part of the selected circuit
parent, not an approximation asserted to equal simultaneous Hamiltonian
evolution.

The initial state on every cluster is `(B+x)^tensor16/sqrt(2^16)`. Opposite
staggered onsite phases are applied to the two cycles, followed by the native
transfer gates with angle `pi/8`, for three depths. The opposite pattern is a
prepared record control that makes the inter-cycle supports active. The first
identical-pattern attempt produced zero inter-cycle current by exchange
symmetry and was rejected before promotion.

## Gate-owned ledger

For each oriented gate `i -> j`, define its operational transported
occupation as `J_g=<q_i>_before-<q_i>_after`. Native two-record transfer
conserves `q_i+q_j`, while onsite gates commute with every `q_i`. Summing each
gate contribution with its two endpoint signs gives the circuit ledger.

The full `2^16=65536` state-vector calculation gives:

- all 24 native supports carry nonzero cumulative current;
- absolute owned throughput is `2.6742223590371705` per cluster;
- maximum absolute connected edge correlation is `0.06592583672451707`;
- expected retained total is `8` per cluster and `128` across L8, within
  `1.5e-14` globally;
- raw ledger residual is `1.66e-14` in L1 and `2.95e-15` in Linf per cluster;
- norm error is `4.45e-16` and record-number-law change is `2.50e-16`.

The terminal instrument remains the inherited complete failure-inclusive
product qutrit/controller/work/reference PVM. The `alpha=r0` preparation
attachment remains adopted F3-MDC input, not a bare-F3 derivation.

## Classification

This is **controlled numerical evidence** for one prepared finite circuit.
The residuals remain raw and unassigned until owner classification. Their
scale beside the norm, number-law, and retained-total controls is consistent
with floating-point accumulation, but they are not declared exact zeros or
physical defects.

The result establishes active transport across a larger connected record
support than the independent rings. It does not establish individual
lineage identity after mixing, a generic retention law, criticality,
continuum behavior, Ward structure, mature macro dynamics, or gravity.
