# R-B L8 density-controlled record accumulation

This packet performs the first routine R-B calculation under the V002
record-first schedule. It compares exact tensor-product write/transport
histories at `L=4` and `L=8` with one disjoint active native carrier edge per
two cells.

Run:

```text
python3 -B DEVELOPMENT_R_GATE_B_L8_DENSITY_CONTROLLED_ACCUMULATION_V001/verify_l8_record_accumulation.py
```

No continuum or gravity-shaped observable is evaluated.
