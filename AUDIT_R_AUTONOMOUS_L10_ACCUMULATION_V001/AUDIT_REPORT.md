# Hostile audit — autonomous L10 accumulation

**PASS_CONDITIONAL_L10_ACCUMULATION.** Independent 1024-state cycle evolution reconstructs all ten L10 rows. The exact census is 1,000 sites, 500 per F3 layer, 250,000 possible links, 100 disjoint C10 cycles, 1,000 selected edges, 500 prepared lineages and retained expectation 250.

Period two gives alternating `Delta q`; continuity determines current up to uniform circulation. Reflection reverses oriented current while preserving the prepared history, forcing that uniform mode to zero, hence `J_i=-Delta q_i/2`. This reconstruction agrees with every direct L4/L6/L8 current in the prior hostile audit and closes `Delta q+J_out-J_in=0` at L10.

All occupation/current vectors, throughput, correlation, L10/L8 ratios, residual, norm, energy and number-law controls reproduce the target. Exact zero-kappa current is zero; displayed sub-threshold values are eigensolver roundoff, not physical current or defects.

The corrected replay arrangement freezes one raw canonical `RESULT.json`, recursively recomputes every field, requires exact discrete/text structure and float agreement within `8e-12`, and does not overwrite compatible last-bit differences. Two consecutive warning-free replays preserve canonical hash `d5bed03d...`. This is deterministic canonical custody, not a claim that degenerate eigensolvers produce byte-identical raw floats. Runtime and RSS are observations of one environment/run, not exact or universal claims; an audit rerun is separately observed. Support, kappa, separate t/tau and clock, content, source routing and complete read remain conditional. No physical grid, defect, continuum, Ward, critical, generic-phase or gravity claim is promoted. No correction is required.
