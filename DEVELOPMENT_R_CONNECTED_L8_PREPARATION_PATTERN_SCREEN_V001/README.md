# Connected L8 preparation-pattern screen V001

This packet holds the connected support, forward owner-once schedule, depth,
and terminal read fixed while comparing four declared onsite preparation
patterns: same, reversed, first-cycle-only, and second-cycle-only.

Run:

```sh
PYTHONWARNINGS=error python3 compute_preparation_screen.py
```

The run rewrites `RESULT.json` and must finish with
`PASS__R_CONNECTED_L8_PREPARATION_PATTERN_SCREEN__16/16`.

Each pattern remains an explicit record label. The screen does not select a
preferred preparation or define an ensemble measure.
