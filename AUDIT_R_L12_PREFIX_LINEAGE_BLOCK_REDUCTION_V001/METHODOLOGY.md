# Hostile methodology for L12 prefix-lineage reduction

This lane is frozen before either preflight output. It imports no development
module, arrays, combinadic routine, basis ordering, or transition map.

The hostile construction uses tuple subsets in reversed lexicographic order,
Pascal recursion for all dimensions, set insertion for admission, and a
rail-2/connector/rail-1 edge order. It checks the claimed embedding as a map
between labelled sets rather than assuming the target's integer layout.

The screen attempts to refute the reduction by finding:

1. a future lineage bit populated before its event;
2. two canonical lineages merged by the embedding or row address;
3. an admission or transport image outside the claimed next block;
4. a content, bandwidth, or owner-once mismatch;
5. a full/prefix operator transition disagreement on exhaustive L4/L6 bases;
6. a terminal amplitude or registered observable not reconstructed by the
   two lineage children;
7. a mismatch in any L12 dimension or resource integer;
8. a custody digest used as a quotient key; or
9. a target/hostile normalized numerical discrepancy above `2e-12`.

Passing certifies an exact finite representation only. It cannot authorize an
L12 history until the separately frozen reduced-history implementation and
L10 benchmark pass. It cannot promote a sector, spectrum, `z=1`, continuum,
emergence, or gravity claim.
