# Result

The bounded active two-cell history has exact transported seam current
`1/2`, exact cell residuals `(0,0)`, and exact global telescoping residual
`0`.  The current is generated by the native F3 carrier-transfer Hamiltonian,
not by an algebraic gluing convention.

Bare F3 does not identify the raw-source-to-writer normalization because the
physical port coefficient is free.  The concrete `alpha=r0` attachment is
therefore retained as an adopted, conditional F3-MDC member.

The finite mission CTP generator is owner-once, but the stationary global gravity
parent is still owner-incomplete.  Its physical descent/Ward residual remains
`UNDEFINED`.  Plan-level Gate A remains open; Gate B and gravity are not
claimed.
