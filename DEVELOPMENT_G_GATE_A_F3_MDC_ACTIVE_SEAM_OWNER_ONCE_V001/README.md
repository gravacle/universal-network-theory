# Gate-A F3-MDC active-seam and owner-once screen

This bounded development packet continues the audited `L=4` single-history
ledger.  It does three things and no more:

1. proves that bare F3 leaves the raw-source-to-writer normalization free, so
   the attachment is not derivable without a physical port law;
2. instantiates the adopted F3-MDC attachment and composes it with the native
   F3 carrier-transfer term on one oriented edge of `G_4`;
3. compiles the finite mission's exact CTP generator owners once and reports both the
   exact discrete ledger residual and the still-open plan-level owner status.

Run from the repository root:

```text
python3 -B DEVELOPMENT_G_GATE_A_F3_MDC_ACTIVE_SEAM_OWNER_ONCE_V001/verify_active_seam_owner_once.py
```

The packet does not close plan-level Gate A, invoke Gate B, derive a Ward
identity, or claim gravity.
