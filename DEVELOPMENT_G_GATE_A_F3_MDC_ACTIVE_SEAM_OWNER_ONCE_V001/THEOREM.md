# Exact active-seam F3-MDC history and fail-closed owner-once compilation

## 1. Claim typing

The following statuses are load bearing.

- **Proved:** the bare-F3 source normalization is non-identifiable; the
  native qutrit transfer has an exact continuity current; the declared
  two-cell history has a nonzero transported seam flux and exact cellwise and
  global ledgers.
- **Adopted:** `F3-MATERIAL-DUAL-CLOSURE` permits one physical port map to
  bind the audited raw source coefficient to the F3 writer.
- **Conditional:** the concrete parent below uses that adopted attachment and
  an ideal piecewise controller.
- **Open:** derivation of the attachment from bare F3; a generic accumulated
  state; the complete stationary gravity-parent owner census, lawful 1PI
  quotient, descent/Ward residual, and every Gate-B-or-later claim.

No lattice or grid is inserted.  The labels below select two adjacent members
of the already-declared periodic family `G_4`; no distance follows from them.
No graviton or Ward axiom is used.

## 2. Rigorous attachment screen

On a fixed writer-content sector, the admitted F3 write generator is

\[
 K_{w\to a}=|x\rangle\!\langle x|_w\otimes
 (|x\rangle\!\langle B|_a+|B\rangle\!\langle x|_a).
\tag{A01}
\]

The micro-action's physical port is a completion slot with an arbitrary real
coupling.  For every real `alpha`, the family

\[
 H_{W,\alpha}(t;\epsilon)
 ={\hbar\alpha\epsilon\over\tau_W}
 \mathbf 1_{[0,\tau_W]}(t)K_{w\to a}
\tag{A02}
\]

is Hermitian, content-exchange covariant, finite, and has the same bare-F3
operator algebra.  It produces pulse area `Phi=alpha epsilon`.  Two unequal
positive values of `alpha` therefore satisfy all bare-F3 structural clauses
but give unequal write probabilities for a common nonzero `epsilon`.
Consequently bare F3 does not determine `alpha`; in particular it cannot
derive `alpha=r_0`.  This is an identifiability proof, not absence-of-search
evidence.

The adopted F3-MDC member fixes

\[
 r_0={14441248\over6075},\qquad \alpha=r_0,
 \qquad \epsilon_\star={\pi\over4r_0},
\tag{A03}
\]

and hence `Phi=pi/4`.  Equation (A03) is a transparent conditional physical
attachment, not a bare-F3 theorem.

## 3. Native transported seam current

Let `a` and `b` be adjacent cells and let their retained carrier qutrits use
the native F3 occupation `q=|0><0|+|1><1|`.  On the selected content sector,
write

\[
 |A\rangle=|x,B\rangle_{ab},\qquad
 |B\rangle=|B,x\rangle_{ab}.
\]

The native carrier term and current from BS07--BS09 are

\[
 H_{ab}=-tT_{ab},\quad
 T_{ab}=|B\rangle\langle A|+|A\rangle\langle B|,
\quad
 J_{ab}=i(|B\rangle\langle A|-|A\rangle\langle B|).
\tag{A04}
\]

Direct commutation gives the operator continuity equations

\[
 {d q_a\over dt}=-{t\over\hbar}J_{ab},\qquad
 {d q_b\over dt}=+{t\over\hbar}J_{ab}.
\tag{A05}
\]

Thus `J_ab` is not an algebraic overlap seam: it is the current owned by the
physical transfer Hamiltonian.

Use a two-stage complete finite history.  During stage W, only (A02) acts at
`a`, with the writer fixed in `x`, the target at `a` blank, and the target at
`b` blank.  During stage T the source is off and only (A04) acts for

\[
 \theta={t\tau_T\over\hbar}={\pi\over2}.
\tag{A06}
\]

After W the occupied branch at `a` has weight `sin^2(Phi)=1/2`.  Stage T
maps `|A>` to `i|B>` on that branch.  Therefore

\[
 \Delta Q_a=0,\quad \Delta Q_b={1\over2},\quad
 \mathcal J_{a\to b}:=\int_W^{W+T}{t\over\hbar}
 \langle J_{ab}\rangle dt={1\over2},\quad W_a={1\over2}.
\tag{A07}
\]

With outward orientation for each one-cell region,

\[
 \Delta Q_a+\mathcal J_{a\to b}-W_a=0,
 \qquad
 \Delta Q_b-\mathcal J_{a\to b}-W_b=0.
\tag{A08}
\]

All other oriented edges are terms-off in this bounded mission.  On the
periodic union, the paired seam contributions cancel once and
`Delta(Q_a+Q_b)-W_a=0`.  The seam current is physically nonzero.

## 4. Finite CTP owner-once generator

For branch `sigma=+,-`, let

\[
 H_\sigma(t)=H_{0,\sigma}+H_{W,\sigma}(t)
 +H_{ab,\sigma}(t)+H_{{\rm controller},\sigma}(t),
 \qquad
 U_\sigma={\cal T}\exp\!\left[-{i\over\hbar}
 \int H_\sigma(t)dt\right].
\tag{A09}
\]

Let `rho_0=|x,B,B><x,B,B|` include the declared controller-ready factors and
let `{E_y}` be the complete terminal qutrit/controller instrument, including
every registered outcome and satisfying `sum_y E_y=I`.  The exact
finite-dimensional closed-time-path generator is

\[
 Z_y[H_+,H_-]={\rm Tr}\!\left(E_yU_+\rho_0U_-^\dagger\right),
 \qquad Z=\sum_yZ_y,
 \tag{A09a}
\]

with connected functional `W=-i hbar log Z` only on a source neighborhood
where the selected normalized `Z` is nonzero.  This operator definition is
the complete finite CTP object; no omitted continuum Berry/kinetic term is
silently presumed.  At equal branches, completeness gives `Z=Tr rho_0=1`.
The owner census is:

| owner | unique term | status |
|---|---|---|
| stationary/free | `H0=0` on the selected degenerate qutrit sector | exact declared zero |
| raw source and writer attachment | `H_W` | conditional F3-MDC, once |
| carrier/seam | `H_ab` and its derived `J_ab` | native F3, once |
| controller/clock/work | explicit factors and piecewise switch in (A09) | conditional ideal port, once |
| state | initial density boundary functional | exact, once |
| measure/read | complete terminal qutrit read | exact, once |
| matching | common content label and oriented admitted edge | exact mission datum, once |
| other spatial edges | zero Hamiltonian terms | exact declared zero |

The source write `W_a` is derived from `H_W` and is not appended to the
generator.  The current is derived from `H_ab` and is not appended either.
This gives the exact finite-mission owner-once ledger residual

\[
 \mathcal R_{\rm ledger}=(0,0),\qquad
 \mathcal R_{\rm global}=0.
\tag{A10}
\]

## 5. Fail-closed plan-level residual

Equations (A09)--(A09a) are complete only for this bounded controlled
mission.  They are
not the stationary global action GK02/CY21 require.  In particular, generic
support/shared-midpoint dynamics, recoil material, stationary state and
measure, moving projector/constraint, retained-field shell, general boundary
apparatus, and lawful connected/Legendre/Schur quotient owners remain without
one common physical formula.  Their values are `UNDEFINED`, never zero.

Accordingly the plan-level output is

`OWNER_INCOMPLETE__PHYSICAL_DESCENT_AND_WARD_RESIDUAL_UNDEFINED`.

`GLOBAL_OWNER_CENSUS.json` compiles all sixteen GK02 action categories once,
plus the quotient status.  It is the requested global compilation result:
the compilation succeeds as a census and fails closed before a physical
global action can be formed.
No absent formula is represented by a numerical zero.

The result advances the discrete Gate-A evidence but does not close
plan-level Gate A and does not authorize Gate B or any gravity claim.
