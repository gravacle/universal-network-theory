# Independent audit: L4 lineage-sensitive continuation

This packet independently validates the frozen L4 mechanism result while
explicitly bypassing the contraction path that emitted Apple/Accelerate
warnings in the target run.

Run from the repository root:

```text
PYTHONPYCACHEPREFIX=/tmp/unt_l4_audit_pycache \
  python3 AUDIT_R_OWNER_ONCE_L4_AUTONOMOUS_LINEAGE_SENSITIVE_CONTINUATION_V001/test_independent_validate.py
```

`INDEPENDENT_RESULT.json` is an atomic, non-overwritten scientific audit
output.  `RESULT.md` gives its bounded interpretation.

The claim ceiling is one lineage-sensitive carrier mechanism at L4.  Nothing
in this packet establishes scaling, curvature, geometry, or gravity.
