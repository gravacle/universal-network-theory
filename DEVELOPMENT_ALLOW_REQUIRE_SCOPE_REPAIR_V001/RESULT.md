# Allow / require / select scope-repair result

## Verdict submitted to hostile audit

The repository's apparent contradiction is repaired by scope, not by deleting
history and not by adding a new physical law.

The exact bounded result is:

> On a frozen parent/fiber, admissibility-preserving internal dynamics changes
> occupancy without redefining the admitted space.  In a larger parent whose
> state space contains carrier fibers and whose law owns inter-fiber
> transitions, a history may change the subsystem's effective admitted fiber
> while preserving the enlarged parent's single global admitted space.

This result preserves the non-circularity guard and reopens carrier formation
only as a well-typed conditional target.  The packet proves no physical
enlarged parent exists.

The sealed alpha `ALLOW/REQUIRE/SELECT` ladder is sufficient repository-native
precedent: `ALLOW` is set membership, `REQUIRE` is domain-relative universal
or singleton necessity, and `SELECT` is a separate possible actualization
law.  Common-basis characteristic projectors commute exactly, but that null
does not determine the algebra of constraint or dynamical generators.

## Integrated dispositions

| object | disposition | surviving scope |
|---|---|---|
| `D-1` | `PRESERVE` | no macroscopic behavior is imposed as a record-level criterion |
| `D-4` | `NARROW` | frozen parent/fixed fiber only |
| `X-2`, `T-21` | `PRESERVE` | fixed-complex result; collective emergence remains open |
| `G-3` | `SUPERSEDE` | retain only fixed-parent Gamma capacity |
| `G-4` | `PRESERVE` | tested carrier and named generators only |
| `F-2` | `PRESERVE` | fixed-carrier correction to occupancy target `F-7` |
| `F-3` | `REOPEN_CONDITIONALLY` | complete enlarged physical parent required |
| `F-7` | `PRESERVE` | fixed-carrier occupancy question |
| `T-II.5` | `REOPEN_CONDITIONALLY` | enlarged parent must own the Hamiltonian/carrier variable |
| `A-GR3` | `NARROW` | shared-origin shape retained without all-level G-3 premise |
| `A-GR4`, `C-77` | `PRESERVE` | existing no-import/open-emergence boundaries |
| `H-1` | `PRESERVE` | larger emergence question remains open |
| `H-7` | `REOPEN_CONDITIONALLY` | enlarged-parent carrier-formation form |
| `H-8` | `SUPERSEDE` | foreclosure concern resolved; historical provenance retained |
| sealed Gate R-C | `PRESERVE` | all finite values, nulls, and stop gates unchanged |

## Downstream consequence

The proposed numerical centerline screen may proceed only if the independent
hostile report for this packet passes.  A finite-window `z=1`-compatible result
would authorize only a native operator-first algebra screen.  Predicate
projectors are exact null controls; they cannot be rescaled into an anomaly.

No result here claims Poincare/Virasoro closure, a continuum limit, a Ward
identity, gravity, or a proof of emergence.

## Promotion rule

This target becomes governing only when
`AUDIT_ALLOW_REQUIRE_SCOPE_REPAIR_V001/INDEPENDENT_HOSTILE_AUDIT.md` records a
passing semantic verdict and the frozen mechanical verifier also passes on the
final hash-pinned manifest.  Without both records this document is an
unpromoted candidate.
