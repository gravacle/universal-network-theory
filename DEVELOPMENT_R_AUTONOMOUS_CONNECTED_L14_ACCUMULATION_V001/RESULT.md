# Connected L14 target result

The same-slice guarded target passes canonical `28/28`. It covers 2,744
sites, 1,372 prepared lineages, 98 connected components, and 4,116 selected
owner-once edges. The expected retained record is `685.9999999999512`.

All 28 internal and 14 connector currents per component are numerically
active. Global total and connector throughputs are `357.6081559482` and
`115.3539255582`. The L14/L12 per-retained ratios are `0.9999998198` and
`0.9999999506`, respectively. They are one finite comparison, not a fit,
trend, convergence, limit, or scaling statement.

The fine raw record-ledger residual is `1.772e-10` L1 and `6.346e-12` Linf per
component. It remains unassigned and is not called a defect. Runtime and RSS
are single-host observations. The target remains unpromoted until independent
hostile audit.

No autonomous support, physical grid, continuum behavior, Ward identity,
phase, graviton, or gravity is claimed.
