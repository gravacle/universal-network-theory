# Conditional connected L14 microscopic record

## 1. Finite census and exact representation

On the same inherited conditional connected family, L14 contains 98 identical
28-site components: 2,744 sites, 1,372 prepared source lineages, and 4,116
selected owner-once edges. The complete order-28 source-preserving finite
group partitions all `2^28 = 268,435,456` component words into exactly
`9,608,050` orbits. Compiled enumeration independently realizes the audited
Burnside count with orbit-size histogram
`{1:4, 2:6, 7:252, 14:41742, 28:9566046}`.

The fixed-width engine stores `201,769,050` active unaggregated transitions in
`2,699,862,058` bytes including row offsets and current codes. Repeated
destinations are intentionally not aggregated. The parallel symmetric-row
action agrees with the hostile-audited scatter action within `1.67e-16`; the
parallel current calculation agrees with the audited serial finite operator
within `5.56e-17` on the deterministic structural probe. These are
implementation cross-checks, not physical claims.

## 2. Refined finite evolution

At conditional `kappa=pi/2`, order-10 Taylor evolution and independent
512/1,024-panel Simpson integrations give state/current refinement differences
`4.894e-16` and `3.165e-11`. All 28 internal and 14 connector currents per
component are active. Fine internal and connector magnitudes are
`0.0882850693841` and `0.0840772052173`.

The retained expectation is `685.9999999999512`. Global total and connector
throughputs are `357.6081559482` and `115.3539255582`. The raw unassigned fine
record-ledger residual is `1.772e-10` L1 and `6.346e-12` Linf per component;
the global L1 sum bound is `1.736e-08`. It is not called a defect.

The adjacent L14/L12 total and connector throughput ratios are
`1.5879626769` and `1.5879628846`. Their per-retained-record ratios are
`0.9999998198` and `0.9999999506`. These are one finite comparison. They do
not establish monotonicity, convergence, a limit, fit, exponent, scaling law,
or critical behavior.

## 3. Failed-closed attempts and resource observation

An unsupported parallel scalar reduction failed during compilation before
evolution and was replaced by fixed-width per-orbit partial arrays plus a
deterministic sum. A later attempt completed the numerical histories but
failed under warnings-as-errors in the final BLAS incidence product before
emitting a result; the explicit algebraically equivalent owner-edge balance
sum replaced it. No failed-attempt value is promoted.

The successful run took `1528.396889875 s` and reached `6,118,227,968` bytes
(`5.6980438232421875 GiB`) maximum RSS on the declared 48 GiB environment.
These are one-run engineering observations, not complexity or scaling laws.

## 4. Claim classes

**Proved:** the inherited finite group/support/source-parity identities,
Burnside orbit count, complete compiled orbit assignment, owner-once
unaggregated-transition equivalence, and finite signed edge-orbit reduction.

**Adopted:** F3-MDC `alpha=r0`.

**Conditional:** connected support, source family, `kappa`, separate hopping
and clock calibration, content, routing, and complete read.

**Empirical/numerical:** L14 evolution, currents, occupations, correlations,
residuals, finite comparators, runtime, and RSS.

**Open:** independent hostile reconstruction; any next accumulation-axis
choice; autonomous support; and every macroscopic response question.

This is a conditional microscopic record, not a continuum or mature
macroscopic dynamics. No physical grid, continuum behavior, Ward identity,
generic or critical phase, graviton, or gravity is inserted or inferred.
