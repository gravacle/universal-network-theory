# Hostile audit: Gate A-P L6 two-body connected interaction

This packet independently audits
`DEVELOPMENT_R_GATE_AP_L6_TWO_BODY_CONNECTED_INTERACTION_V001` without
importing or executing its code. It is bounded to L6.

The verifier reconstructs the 12-site, 18-edge degree-three prism; builds all
ten stored history labels covering the four histories for each canonical pair
`A0-A1`, `A0-A2`, and `A0-A3`; acts on complete 4,096-word vectors by raw
owner-edge bit swaps; propagates directly to `kappa=pi/2`; and uses nested
256/512-interval Simpson rules for every oriented current integral. This is
independent of the target's 79-dimensional eigensystem and analytic spectral
current integral.

Run:

```sh
PYTHONWARNINGS=error python3 -B \
  AUDIT_R_GATE_AP_L6_TWO_BODY_CONNECTED_INTERACTION_V001/hostile_audit.py
```

The sealed result must finish:

```text
PASS__AUDIT_R_GATE_AP_L6_TWO_BODY_CONNECTED_INTERACTION__172/172
```

The audit also checks the new bounded single-source ledger paragraph. It
recomputes the stored binary64 L12 shell ratio, verifies the prism/torus type
distinction, and repeats the amplitude-free cubic-torus comparator screen.

The passing result establishes only a finite conditional L6 connected
response. Nonzero connected current rejects strict observable superposition
for this protocol but is not automatically an exchange potential. The
connected Hamiltonian expectation is `-1/2` only at direct contact and zero
at `d=2,3`; this does not establish mutual binding. Gate A-P remains open.
