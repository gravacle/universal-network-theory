# Independent hostile audit report

## Disposition

**PASS, 172/172.** The target and its antecedents are pinned by SHA-256 in
`INDEPENDENT_RESULT.json`. The audit found no material numerical,
topological, ledger, or claim-boundary discrepancy. No support larger than L6
was executed.

The audit used a genuinely separate numerical route. The target diagonalizes
the complete reachable 79-dimensional zero/one/two-particle restriction and
integrates currents analytically. The audit instead represents every history
as a full 4,096-word vector, applies the Hamiltonian through direct raw bit
swaps on all words, advances by an order-eight polynomial on 512 intervals,
and integrates all 18 oriented currents with nested 256/512-interval Simpson
rules.

Observed audit runtime was `4.389104667 s`; maximum resident memory was
`37,175,296 bytes` on the recorded 48 GB environment. A separate final replay
of the target itself passed `25/25` in `0.043542000 s` with `30,654,464 bytes`
maximum RSS.

## Numerical evidence

The independently reconstructed edge order, source-zero BFS distances, and
degree census exactly match the 12-site, 18-edge degree-three prism. The
terms-off ledgers remain exact:

```text
single write:  1/2 + 0 - 1/2 = 0
double write:  1   + 0 - 1/2 - 1/2 = 0
```

All target inclusion-exclusion rows rebuild exactly with signs
`O11-O10-O01+O00`. Across all complete history-current vectors, the fine
full-word result differs from the target spectral analytic result by at most
`1.509e-11`; coarse/fine separation is `2.263e-10`. Across the 54 connected
current components, the maximum target difference is `5.243e-12`.

| d | independent `Delta12 E_final` | independent `sum abs Delta12 J` | current-vector target Linf | connected-ledger L1 |
|---:|---:|---:|---:|---:|
| 1 | `-0.4999999999999998` | `0.26695732547100265` | `1.524e-12` | `1.680e-11` |
| 2 | `0` | `0.8946409434974187` | `5.243e-12` | `1.292e-10` |
| 3 | `-9.645e-16` | `0.2936201703167873` | `2.146e-12` | `2.028e-11` |

The independent connected-ledger remainders are quadrature-controlled raw
remainders, not new physical terms. Their maximum L1 value is `1.293e-10`.
Terminal occupation vectors agree with the target within `2.221e-15`, final
energies within `9.912e-16`, and energy conservation within `9.646e-16`.

## Hostile conceptual gate

The finite nonzero `Delta12 J` vectors reject strict additive observable
superposition for the declared coherent four-history protocol. They do not,
without a separation-dependent energy or force construction, establish an
exchange potential.

The connected energy is exactly a direct-contact term initially:
`Delta12 E(1)=-1/2`, while `Delta12 E(2)=Delta12 E(3)=0`. Conservation under
the common Hamiltonian retains that pattern. One negative contact value plus
two zero noncontact values does not show mutual attractive binding. The target
states both limitations correctly.

The all-blank vacuum parent, common phase, canonical A-ring orientation, and
write/transport schedule remain conditional. Other pair orbits and phases,
larger supports, physical distance, a noncontact potential, binding, and Gate
A-P remain open. No grid, continuum behavior, Ward identity, phase, graviton,
or gravity is promoted.

## Single-source ledger paragraph

The L12 ratio is reproduced directly from the pinned stored values:

```text
0.7570375735011285 / 0.0005258194763641394
  = 1439.7290468123826  (binary64)
```

Radius two is the shell-L1 maximum in every L6--L12 row. The ledger properly
labels “exponentially screened” as an adopted bounded empirical description,
not an exact exponential, asymptotic law, mass, or continuum result.

The carrier shells are shortest-path edge shells on a `2L`-site, `3L`-edge,
degree-three prism. The external comparator is a scalar Green kernel on an
`L^3`-site, degree-six cubic torus. No physical map between them is supplied,
so the latter is comparator-only.

After normalization at `r=2`, the strict-interior comparator residuals are:

| record/comparator | RMS | maximum absolute residual |
|---|---:|---:|
| signed / torus axis | `0.108652` | `0.213329` |
| signed / torus Manhattan-shell mean | `0.261264` | `0.368303` |
| L1 / absolute torus axis | `0.0682613` | `0.146311` |
| L1 / torus Manhattan-shell mean | `0.165300` | `0.218461` |

Axis-kernel signs also disagree with the positive signed record ratios at
`(L,r)=(8,4)`, `(10,5)`, and the additional farthest shell `(12,6)`. The
ledger's negative non-torus screen is therefore supported, but it neither
selects an alternate microscopic law nor makes a continuum claim.

## Discrepancies

None material. The independent Simpson ledgers are less numerically exact
than the target's analytic spectral integrals, as expected, but converge under
the recorded coarse/fine control and agree componentwise with the target well
inside the audit thresholds.
