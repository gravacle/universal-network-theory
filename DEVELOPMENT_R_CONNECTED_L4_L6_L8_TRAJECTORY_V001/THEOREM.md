# Connected native-component L4-L6-L8 trajectory — numerical result

## Scope and parent custody

This packet hash-pins the hostile-audited L4/L8 parent result and inserts one
independently computed L6 record under the identical labels: paired native
generator cycles, reversed onsite preparation, forward owner-once schedule,
transfer angle `pi/8`, depth three, inherited adopted `alpha=r0`, and the
complete failure-inclusive terminal product read.

At L6, nine 12-record components partition all 108 prepared retained heads.
Each component has 18 unique native supports and 54 owner-once gate events;
all 18 cumulative supports, including all six inter-cycle supports, are
active. The finite tuple labels are inherited enumeration, not a physical
grid or distance.

## Three raw records

| quantity per retained head | L4 | L6 | L8 |
|---|---:|---:|---:|
| gate-event absolute traffic | 0.1873029450 | 0.1819357914 | 0.1831009165 |
| cumulative net edge throughput | 0.1621539198 | 0.1658328077 | 0.1671388974 |
| cumulative net inter-cycle throughput | 0.0608123878 | 0.0585992499 | 0.0592350627 |
| max abs connected edge correlation | 0.0506661854 | 0.0644117544 | 0.0659258367 |

The adjacent per-head ratios are:

| observable | L6/L4 | L8/L6 |
|---|---:|---:|
| event traffic | 0.9713450658 | 1.0064040454 |
| net edge throughput | 1.0226876285 | 1.0078759431 |
| net inter-cycle throughput | 0.9636071206 | 1.0108501878 |

Event and inter-cycle traffic are non-monotone across the three records; net
edge throughput rises across them. These are raw facts at three finite sizes,
not a fitted trend, exponent, convergence statement, or critical signal.

Expected retained totals are `16`, `54`, and `128` within numerical closure,
matching the prepared census. At L6 the per-component raw ledger residual is
`5.995204332975845e-15` in L1 and `1.1657341758564144e-15` in Linf; its tiled
L1 bound is `5.395683899678261e-14`. Norm and record-number-law errors are
`1.23e-15` and `2.23e-16`.

## Classification

This is **controlled numerical evidence** for three explicitly labeled finite
record histories. It resolves neither the separately audited schedule and
preparation dependence nor a selection measure over histories.

No interpolation formula, scaling exponent, limit, generic accumulation law,
criticality, continuum behavior, Ward structure, mature macroscopic dynamics,
or gravity is derived. Raw residuals remain unassigned pending owner
classification; they are not physical defects or exact zeros.
