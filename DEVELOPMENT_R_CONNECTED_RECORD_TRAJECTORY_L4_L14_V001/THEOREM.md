# Append-only connected record trajectory through L14

The sealed L4/L6/L8/L10/L12 trajectory is the base object. This packet pins
its complete result and the separately hostile-audited L14 target/audit,
retains all five prior rows and four prior adjacent comparators as parsed data,
and appends only the L14 row and L12-to-L14 comparator.

The appended finite row records 2,744 sites, 1,372 prepared lineages, 98
components, 4,116 owner-once edges, and retained expectation
`685.9999999999512`. Global total/connector throughput is
`357.6081559482/115.3539255582`; per retained record it is
`0.5212946880/0.1681544104`. The adjacent L12-to-L14 total/connector ratios are
`1.5879626769/1.5879628846`; the corresponding per-retained ratios are
`0.9999998198/0.9999999506`.

These are raw finite values and one adjacent comparison. They are not a fit,
trend, monotonicity statement, convergence result, limit, exponent, or scaling
law. The six separately generated residual rows remain raw and unassigned;
they are not renamed defects or used as fit inputs.

The compilation changes no physics. Source, support, `kappa`, routing,
content, clock, and read remain conditional. It adds no autonomous support,
physical grid, continuum behavior, Ward identity, phase, graviton, or gravity.
