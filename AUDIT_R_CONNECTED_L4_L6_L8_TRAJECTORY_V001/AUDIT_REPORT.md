# Hostile audit — connected L4/L6/L8 trajectory

**PASS_CONTROLLED_NUMERICAL_L6_INSERTION.** The hostile-audited L4/L8 parent result is hash-pinned. A new target-independent 4,096-state L6 reconstruction gives 9 twelve-site components partitioning 108 heads, 18 unique native supports, 6/6 active rungs, and 54 owner-once events per component. Expected retained total is 54 within numerical closure. Traffic, cumulative-net and inter-cycle values, correlation, ledger residuals, norm, and number-law controls reproduce the target.

Adjacent total and per-head ratios were recomputed from the pinned parent rows and independent L6 row. Event and inter-cycle traffic per head fall L4→L6 then rise L6→L8; net edge throughput per head rises on both intervals. This mixed behavior is raw three-size data only—not an interpolation, exponent, convergence, critical signal, or generic law.

The reversed preparation and forward schedule remain selected-history labels. The read is complete failure-inclusive; attachment is inherited adopted `alpha=r0`. Coordinates are finite enumeration, not a physical grid; residuals are unassigned rather than defects. No continuum, Ward, or gravity claim is promoted. No material defect or repair was found.
