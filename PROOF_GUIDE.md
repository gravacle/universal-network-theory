# Proof guide compatibility pointer

The former repository proof guide has been consolidated into the
[Universal Network Theory Major Proof Index](publication/zenodo_reproduction_v001/UNIVERSAL_NETWORK_THEORY_MAJOR_PROOF_INDEX.md).

The controlling composition theorem is the
[Universal Network Theory Closure Theorem](UNIVERSAL_NETWORK_THEORY_CLOSURE_THEOREM_V001.md).

Use that master document for the dependency-ordered reading path, major proof
catalogue, external citations, exact results, claim typing, reproduction
commands, Git-versus-Zenodo boundary, and release checklist. The theorem,
audit, ledger, result, and dated provenance files cited by the master index
remain authoritative evidence and have not been replaced by the index.

## Current finite-evidence addendum — 2026-09-25

The live generated proof and ledger now also register the independently
reproduced owner-once joint lineage--carrier witness at `L = 10` and `L = 12`
as row `RA-9`. The two-sided association is nonzero at both held-out sizes and
the independent implementations agree, while the separately frozen signed
positive no-decline criterion fails because the witness reverses sign and
declines in magnitude. This result is finite computational evidence only. It
does not alter the finite ARGER Gate, conditional dynamical-`z=1`, RGRL/WTC,
alpha, geometry, or gravity proof types.

The `10,748 s` branch-release skew is disclosed as a deviation from a legacy
`60 s` anti-contamination rule, so the result is not described as a pristine
strict-protocol held-out pass. The branch chronology and unopened-value barrier
leave the deterministic scientific agreement intact. See `REGISTER_V001.md`
for the live protocol-policy correction; the historical freeze is unchanged.

The live proof also registers `RA-10`, the prospectively frozen L8
formation-order-path Ollivier--Ricci controlled null. Its exact result is
`rho=0.1543033499620919`, `p_plus=0.3619047619047619`, so no association is
resolved at the registered sensitivity. The null is scoped to that unweighted
schedule-derived path; it neither rules out richer prospectively earned
lineage graphs nor supplies emergent geometry, spacetime curvature, or gravity.

Row `RA-11` and proof block `P-CURRENT-9` register the independently
reproduced fixed L4 autonomous continuation. A same-sector product comparator
with equal separate lineage and carrier marginals receives the same frozen
lineage-reading revisit and carrier transport as the retained joint state. The
independent route gives carrier trace distance
`Delta_C=0.14761185701903007` at `tau=1e-10` and occupation-profile RMS
`0.006009875541368592`, with zero captured warnings and target/independent
agreement to `1.1102230246251565e-16`. Common-transport trace-distance
invariance is a control. The claim is one finite L4 mechanism only; it supplies
no size persistence, scaling, curvature, geometry, RGRL/WTC, alpha, or gravity.
