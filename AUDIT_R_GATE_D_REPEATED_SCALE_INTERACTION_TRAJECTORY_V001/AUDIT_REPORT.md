# Independent hostile audit

## Disposition

`PASS_CONDITIONAL_FIXED_DENSITY_TRAJECTORY`.

For every tested even `L` (2, 4, 6, 8, 10), odd first-coordinate cells number
`L^3/2`. Pairing even to odd second coordinates produces `L^3/4` disjoint
blocks and covers every prepared head once. The same counting proof applies
to arbitrary positive even `L`.

Each inherited R-C block contributes retained occupation one, absolute
oriented throughput `1/4`, positional expectations `1/4` and `3/4`, and zero
endpoint residuals. Therefore the exact general rows are

- blocks and retained: `L^3/4`;
- throughput and low position: `L^3/16`;
- high position: `3L^3/16`; and
- record-ledger residuals: `(0,0)`.

At L4 this gives `(cells,blocks,retained,throughput,low,high,r1,rinf) =
(64,16,16,4,4,12,0,0)`. At L8 it gives
`(512,128,128,32,32,96,0,0)`. All six nonzero raw ratios are exactly eight.

The attachment is explicitly inherited as adopted
`alpha=r0=14441248/6075`, not derived from bare F3, not refitted, and unchanged
with `L`. The terminal instrument includes complete qutrit, controller
`OK/FAILURE`, work, and reference effects at both sizes; failure outcomes stay
in the alphabet without conditioning. The scope remains a conditional,
fixed-density preparation of mutually disjoint identical blocks.

The factor eight is algebraic volume replication. It is not a fitted scaling
exponent, evidence of criticality, a continuum limit, generic accumulation,
overlapping-interaction stability, individual lineage persistence, Ward
structure, or gravity. The packet states these ceilings and leaves a future
nonzero residual unclassified pending owner analysis.

No material defect was found at the declared scope.
