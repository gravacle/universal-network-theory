# Scalable Relational Accumulation Screen V001

**Status:** `FROZEN_CANDIDATE__PRE_OUTPUT`

**Sealed input:** `3c8e8385a1113941b545276410e7bc2e340e2a84`

## 1. Objective

Run a background-structure-free finite version of the original Authenticated
Accumulation Screen after the L4 intrinsic-admission repair. The protocol asks
whether one autonomous, owner-complete internal genesis history produces a
common finite density interval across L4--L12 and, only if it does, whether
that entire interval contains a stable response-active `z=1` candidate.

The calculation may identify a finite structural candidate. It cannot prove a
thermodynamic phase, continuum background independence, Poincare/Virasoro
closure, universal coupling, metric dynamics, emergence, or gravity.

## 2. Complete relational parent

For each even `L in {4,6,8,10,12}`, retain the owner-once degree-three periodic
prism with `2L` carrier vertices and `3L` transport edges. Every carrier vertex
has the same three-state admission cell `I,R,S` from the sealed L4 parent.
The initial condition selects one connected L-cell cycle as loaded `R`; all
other cells are `I`, and every retained carrier vertex begins blank. This
loaded cycle is a relational dense genesis subgraph. It is not an external
reservoir or privileged boundary.

The internal cursor is a basis-state token on the adjacent Hamiltonian cycle

```text
(0,0)->...->(0,L-1)->(1,0)->(1,L-1)->...->(1,1)->(0,0),
```

where the two rail-to-rail steps use owner connectors. The initial cursor
orientation is adopted initial data. The same fixed update is iterated:

```text
U_rel = S_cursor U_T U_A(cursor),
U_A(cursor) = exp[-i (pi/4) K_rel(cursor)],
U_T = exp[-i (pi/2) H_T].
```

`S_cursor` is the unitary permutation to the next adjacent cursor state. The
calculation is indexed by cursor transitions, not by an added physical global
clock. Stop after the first `L` transitions, before any loaded/spent cell can
be revisited.

At a loaded cell `w` and its co-located retained target `r`,

```text
K_rel(w,r) = |S,x><R,B| + |R,B><S,x|.
```

The occupied target `|R,x>` and every inactive-cell state are exact kernel
states. Accepted branches consume the cell's content and bandwidth and seal
the `(w,r)` lineage. Define

```text
Q_total = Q_retained + number_R,
B = number_R,
L_sealed = L - number_R.
```

`Q_total=L` is exact on the complete parent. `ALLOW` is represented by the
`R,B` characteristic projector, `REQUIRE` audits the displayed branchwise
cost/lineage implications, and `SELECT` names the trajectory generated by
`U_rel`. None of those predicates supplies `K_rel`, `H_T`, or `S_cursor`.

## 3. Exact reachable representation

The inactive cells and definite cursor factor out on the selected one-pass
history. Retain all basis states of the `L` active loaded/spent cells and `2L`
carrier modes satisfying total content `L`. The exact dimension is

```text
D_L = sum_q binomial(L,q) binomial(2L,q) = binomial(3L,L).
```

| L | `D_L` | one complex128 state |
|---:|---:|---:|
| 4 | `495` | `7.38 KiB` |
| 6 | `18,564` | `0.283 MiB` |
| 8 | `735,471` | `10.96 MiB` |
| 10 | `30,045,015` | `0.448 GiB` |
| 12 | `1,251,677,700` | `18.65 GiB` |

Store the state by genesis mask and retained sharp-number block. Admission
couples adjacent `q` blocks; native transport acts independently inside each
genesis mask. L10/L12 must use streamed blocks and adaptive Krylov action; a
dense global matrix is forbidden. No truncation, branch deletion, or
unreported tensor compression is allowed. If exact storage or the required
correlations exceed the guards below, stop and report the obstruction rather
than substituting an approximate macro closure.

## 4. History observables and ledgers

For every cursor event `n=1,...,L`, retain:

```text
a_L(n) = Pr(ALLOW at the fresh cell) = 2 W_n,
b_L(n) = blocked occupied-target probability,
p_L,n(q) = Pr(Q_retained=q),
rho_bar_L(n) = sum_q q p_L,n(q)/(2L),
B_L(n), L_sealed_L(n),
```

and the complete admission and native transport ledgers. A counterfactual
null-admission branch begins from the exact same pre-event state and supplies
the connector redistribution

```text
delta J_e,L(n) = J_e(actual)-J_e(null admission),
j_L(n) = sum_connector |delta J_e,L(n)| /
         sum_connector |delta J_e,L(1)|.
```

Retain the signed connector sum separately. Define the descriptive routed
acceptance `c_L(n)=min(a_L(n),j_L(n))`. Unlike the rejected same-port protocol,
this finite one-pass resource does not preregister a 0.75/0.25 depletion
crossing. `c_L` is reported, not used to manufacture a critical point.

Every event requires:

```text
W_n >= -epsilon_L,
blocked-null error <= 1e-11,
reverse support before first use <= 1e-11,
admission accounting residual <= 1e-10,
transport node-ledger L1 residual <= max(1e-9,100 epsilon_L),
norm and total-content error <= 1e-10.
```

Here `epsilon_L` is `max(1e-11,50*d_L)`, where `d_L` is the maximum target
coarse/fine or target/blind disagreement across state, sector, admission, and
current-quadrature observables. L4 must reproduce the sealed four-event
admission observables within `1e-9`.

## 5. Accumulation-generated finite sector

The reporting window is fixed by cursor progress, not by density:

```text
H_L = {n : ceil(L/2) <= n <= L}.
```

Average the actual history weights

```text
pbar_L(q) = |H_L|^-1 sum_(n in H_L) p_L,n(q).
```

Let `[q_-(L),q_+(L)]` be the shortest contiguous integer interval containing at
least `0.99` of `pbar_L`; break ties by larger mass and then lower `q_-`. Its
density-cell envelope is

```text
I_L = [(q_--1/2)/(2L),(q_++1/2)/(2L)] intersect [0,1].
```

First run independent target/blind histories at L4, L6, and L8. If any history
is unresolved, has negative `W_n`, lacks dynamically nonzero blocking, or the
connected intersection `I_4 intersect I_6 intersect I_8` is empty, halt before
L10/L12. Otherwise freeze every raw history, interval, and hash before opening
L10/L12.

After L10/L12, define

```text
I_acc = intersection_(L=4,6,8,10,12) I_L.
```

An empty or boundary-only intersection returns
`NO_COMMON_RELATIONAL_ACCUMULATION_SECTOR_L4_L12`. A nonempty interval is a
finite candidate support only; it is not called a phase or critical density.

## 6. Complete interval Krylov sweep

Partition `I_acc` at every sharp-sector cell boundary `(q+1/2)/(2L)`. Every
positive-width density atom selects exactly one `q_L` at each size. Sweep all
atoms; no best-density choice is allowed.

Use the previously frozen total-rail `m=1` response channel:

```text
Delta_act = lowest positive pole with authenticated response weight,
chi_tau   = sqrt[sum_a w_a/omega_a^2 / sum_a w_a],
R_low     = w_(Delta_act)/sum_a w_a.
```

L4/L6/L8 use complete sector spectra. L10/L12 use independently constructed
translation blocks and fully reorthogonalized response-cyclic Krylov spaces,
retaining the lowest five reported Ritz values. Reuse the sealed residual,
Hermiticity, orthogonality, pole-threshold, 128-vector, and 2,000-matvec guards.

For every atom fit

```text
fixed-z1:      a/L + b/L^2,
free-gapless:  a L^(-z),
positive-gap:  Delta_inf + a L^(-z).
```

An atom passes exactly when:

1. `Delta_act` strictly decreases and `chi_tau` strictly increases with L;
2. fixed-`z=1` held-out SSE is below positive-gap SSE and no greater than
   `1.25` times free-gapless SSE;
3. fitted `z,y in [0.90,1.10]` and `|z-y|<=0.10`;
4. over L8--L12, relative ranges of `L Delta_act` and `chi_tau/L` are at most
   `0.05`;
5. `R_low>=1e-6` at every size and its L8--L12 relative range is at most
   `0.35`; and
6. every target/blind numerical and sector identity check passes.

A connected passing component is an
`AUTHENTICATED_RELATIONAL_Z1_INTERVAL_CANDIDATE` only if it includes at least
two adjacent L10 sectors and two adjacent L12 sectors and carries at least
`0.50` of every `pbar_L`. Otherwise the result is
`AUTHENTICATED_RELATIONAL_Z1_REJECTED_L4_L12`.

Compatibility is a finite structural candidate, not an exact `z=1` theorem.
It would authorize only a separately frozen native-operator algebra screen.

## 7. Independent hostile requirement

Before each target stage, freeze a second implementation that does not import
target code or matrices. It must reconstruct the relational parent, history
blocks, ledgers, sector weights and intervals, spectral atoms, Krylov records,
fits, and every Boolean disposition. Required target/blind agreement is
`1e-8` for history observables and sector weights, `2e-8` for energies/gaps,
`5e-7` for `chi_tau`, and `5e-6` for residues. Independent hostile review is
required before promotion.

## 8. Resource and escalation guards

- Seed wave: L4/L6/L8 only; halt unless its common interval passes.
- L10 history: at most 4 GiB RSS, 8 GiB scratch, and 2 wall hours per method.
- L12 history: at most 40 GiB RSS, 30 GiB persistent state/scratch, and 6 wall
  hours per method. Begin only after confirming at least 60 GiB free scratch
  storage and the L4--L10 gate.
- Spectra: 6 GiB per process, 32 GiB aggregate, 128 Krylov vectors, 2,000
  matvecs, and 3 wall hours per sector.
- Abort rather than relax a threshold, drop lineage, discard a branch, or add
  an unowned controller/reservoir.
- If the required exact state or boundary record exceeds these guards, return
  `EXACT_RELATIONAL_LINEAGE_SCALING_OBSTRUCTION` for theory review.

**Proved inputs:** Gate A-R owner-once conservation, the sealed L4 intrinsic
blocked-null theorem, and the prism's finite topology. **Adopted:** genesis
cluster, cursor cycle/orientation, one-pass length, pulse, dwell, late-history
window, sector mass, response channel, thresholds, and fit families.
**Conditional:** the finite enlarged parent as a candidate accumulation
engine. **Empirical:** all future histories, intervals, spectra, and fits.
**Open:** a self-replenishing autonomous genesis law, thermodynamic scaling,
native spacetime algebra, macroscopic closure, universal coupling, long-range
attraction, metric/action dynamics, emergence, and gravity.

No external boundary, reservoir, absolute global clock, grid, graviton, Ward
axiom, continuum assumption, or gravity claim is inserted.
