# Independent hostile audit of R-B and schedule V002

## Disposition

`PASS_AFTER_REPAIR`. The finite arithmetic, throughput definition,
conditional scope, complete raw read, attachment provenance, microscopic
residuals, and R/M schedule separation are defensible.

## Independent exact census

For each even `L`, the edges `(2r,y,z)->(2r+1,y,z)` are disjoint and cover all
`L^3` cells. Thus there are `L^3/2` modules: 32 at L4 and 256 at L8. Each
audited module has write `1/2`, module-oriented integrated current `1/2`,
final retained occupation `1/2`, and zero tail/head residuals.

The repaired quantity
`A_L=sum_e |integral (t/hbar)<J_e> dt|` is an absolute module-oriented
throughput. It equals 16 and 128. It is not net flux: every active edge is
internal to the periodic family, so the net periodic boundary flux is zero.
The revised theorem and result JSON now distinguish these facts correctly.

Tensor-product Bernoulli statistics give expected retained totals 16 and
128, variances 8 and 64, density `1/4`, and L8/L4 total and lineage ratios
eight. These are exact outputs of the declared preparation, not sampled
empirical evidence.

## Scope and genericity

The target repeatedly labels the family conditional, tensor-product,
fixed-density, and prepared. It explicitly denies spontaneous formation,
generic-state accumulation, interaction stability, a phase, and continuum
behavior. Its candidate R-B pass is only the first prepared accumulation
census. This does not overclaim generic accumulation.

## Repaired read and attachment custody

The repaired target explicitly pins
`alpha=r0=14441248/6075`, `epsilon_star=pi/(4r0)`, and states that this
conditional F3-MDC attachment is neither derived from bare F3 nor refitted at
L8. Replication therefore does not silently recalibrate the larger family.

Each module now supplies controller effects `{E_OK,E_FAILURE}` resolving the
controller identity, retains the zero-probability failure outcome, and takes
the product with complete qutrit, work, and reference PVMs. The full product
resolves the declared finite factor identity and preserves the raw outcome
string without success conditioning. This closes the prior read-owner defect.

The renamed `r1` and `r_infinity` are sums/maxima of exact cell residuals. Both
are zero at L4 and L8. They are correctly described as microscopic
record-ledger residuals. A future nonzero is unclassified and could identify a
necessary owner or collective term; it is not prejudged as a defect. The
continuum Ward defect remains a later M-series diagnostic.

## Execution custody

The custody arithmetic is internally exact: `51,539,607,552` bytes is 48 GiB;
`11,075,584 / 2^20 = 10.5625` MiB; and
`7,110,968 / 2^20 = 6.781547546386719` MiB. Wall/user/system seconds are
nonnegative (`0.07/0.02/0.01`), and the record reports zero swaps. Maximum RSS
and macOS peak-footprint are distinct operating-system metrics, so their
ordering is not treated as an arithmetic contradiction.

These measurements belong only to the exact combinatorial prepared-history
verifier that enumerates 512 labels and 256 disjoint modules. The custody note
and theorem correctly deny allocation or evolution of a dense `3^512` state
vector. Runtime and memory therefore establish reproducible verifier custody,
not feasibility of dense many-body evolution or a physical scaling law.

## Schedule V002

`RECORD_FIRST_ACCUMULATION_GATE_PLAN_V002.md` cleanly separates R-series
record questions from optional M-series macro response. It does not impose
continuum exponent, correlation length, Ward, massless-mode, or gravity
behavior on R-B. Its owner-once rules remain strict: every present exchange
must be owned, while an absent optional macro degree of freedom creates no
record debt. Prepared tensor products cannot prove generic accumulation.
This schedule review passes and does not weaken owner accounting.

The R-B target remains repairable without changing old authoritative plan or
continuation files. No M-series, Ward, Gate-A gravity, or gravity promotion is
licensed.
