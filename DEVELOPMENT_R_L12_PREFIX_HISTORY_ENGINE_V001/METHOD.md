# Target prefix-history engine V001

**Status:** `FROZEN_CANDIDATE__PRE_CONTROL_OUTPUT`

**Inputs:**

- commit `42f1ea3301ccf98802c07f3330d52999385cba0b`;
- the exact prefix theorem in
  `DEVELOPMENT_R_L12_PREFIX_LINEAGE_BLOCK_REDUCTION_V001/`; and
- the physical/numerical controls in
  `DEVELOPMENT_R_SCALABLE_RELATIONAL_ACCUMULATION_V001/`.

This engine changes only the lineage-row allocation for L4--L10. Before event
`n` it materializes exactly `H_n`; after admission it materializes exactly
`H_(n+1)`. It uses the sealed target carrier-sector Hamiltonian, adaptive
Lanczos resolutions, current quadratures, observables, and summary gates
without modification. The target Krylov batch ceiling remains the V002 value
`1,000,000,000 B`.

The engine is first limited to L4/L6/L8 controls. After those pass, its same
frozen bytes may run L10 once. It refuses L12: the separately screened
terminal stream and large-carrier memory schedule must be attached in a later
pre-output method supplement. No L12 output may be produced by this file.

Control comparison tolerances are the frozen history tolerances: `1e-8` for
registered observables and sector weights, with every original ledger,
positivity, reverse-support, blocking, norm, and convergence gate retained.
The L10 benchmark additionally must pass the unchanged `4 GiB` RSS and
`7,200 s` wall limits and match the sealed V002 target within `1e-8`.

Passing L10 authorizes only freezing the L12 terminal/memory supplement. It
does not itself authorize a spectrum, `z=1`, continuum, emergence, or gravity
claim.
