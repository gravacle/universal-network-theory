# Hostile audit — connected prism-orbit reduction

The order-`2L` finite reduction passes without correction. The independent implementation starts from the relabeling `x=i-a`, generates the group from a unit rotation with parity-linked layer swap and a zero-shift reflection, and uses generator BFS rather than the target's explicit `2L` image set. It verifies support and original-site-parity preservation at L4/L6/L8/L10. This establishes a finite subgroup only; neither maximality nor physical geometry is granted.

Independent orbit dimensions are `55/430/4435/53764`; every size histogram and quotient nonzero count `184/3044/49076/786300` matches the target. The quotient is assembled from destination-side incoming multiplicities rather than target source representatives and has zero Hermiticity mismatch. Group transport produces exactly two consistent signed oriented-edge orbits and reconstructs every stored edge orientation.

Independent RK4(4096)+Simpson evolution, distinct from Taylor-10, checks every sealed occupation and signed current. Maximum occupation differences are `1.87e-13/5.33e-13/1.11e-12/1.90e-12`; current differences are `4.53e-14/2.68e-13/4.23e-13/6.30e-13`. Direct continuity, norm, energy, and number-sector controls pass. Exactness stops at finite group/orbit/quotient/current-reconstruction identities; dynamics and quadrature remain numerical.

An independent structural-only L12 enumeration confirms `16,777,216 -> 704,370`, histogram `{1:4, 2:6, 3:12, 4:30, 6:142, 8:15, 12:10316, 24:693845}`, 12,582,508 quotient entries, and 201,320,128 operator-array bytes (`191.99383544921875 MiB`) under the stated 16-byte-per-entry representation. It performs no L12 evolution. The target's `0.013946 s` action and `1,703,739,392`-byte RSS are unreplicated one-run host observations, not complexity bounds or L12 physics evidence.

“Prism” remains a finite graph relabeling, not a grid or geometry. Support/source/kappa/routing/content/clock/read remain conditional. No maximal group, autonomous support, defect, continuum, Ward, critical or generic phase, graviton, or gravity is promoted. No correction required.
