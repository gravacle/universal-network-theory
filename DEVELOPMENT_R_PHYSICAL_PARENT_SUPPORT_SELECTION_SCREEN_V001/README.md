# Physical-parent support-selection screen V001

This packet asks whether the support used by the audited autonomous carrier
record is selected by the unchanged F3 parent. It adds no support law.

Run:

```sh
PYTHONWARNINGS=error python3 compute_support_selection_screen.py
```

The run rewrites `RESULT.json` and must end with
`PASS__R_PHYSICAL_PARENT_SUPPORT_SELECTION_SCREEN__29/29`.

The exact result is a non-selection boundary. BS06 can energetically favor
the degree-two class, but it cannot select the labeled L4/L8 cycle cover.
BS09, BS10, and BS11 conserve incidence; FPSS conditionally compiles a supplied
orthogonal program and retains all such words without preference. No grid,
continuum, Ward, phase, or gravity inference is made.
