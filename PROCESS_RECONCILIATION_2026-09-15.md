# Process Reconciliation — 2026-09-15

> **SUPERSEDED PROCESS SNAPSHOT — 2026-09-16.** This document preserves the
> pre-adoption process and pending-L14 plan; it no longer governs reconciliation
> or release order. Use [`README.md`](README.md) and
> [`PROOF_GUIDE.md`](PROOF_GUIDE.md), the adopted finite Gate in
> [`ARGER_GATE_ADOPTION_2026-09-16.md`](ARGER_GATE_ADOPTION_2026-09-16.md),
> and the terminal L14 disposition in
> [`L14_RUN_DISPOSITION_2026-09-16.md`](L14_RUN_DISPOSITION_2026-09-16.md).
> L14 is `INCOMPLETE_PRESERVED_NO_SCIENTIFIC_RESULT`.

## Purpose

This document is the current process companion to the Universal Record Model. It does not rewrite
the sealed historical `PROCESS_V002.md`; it states how the post-September-4 results enter the live
model, proof guide, Git history, and reproduction packet.

## Two related routes

The repository now carries two related but logically distinct routes:

1. **Record-first conditional closure.** The authoritative Gravity Formation Theory statement is
   `GRAVITY_RECORD_FIRST_WORKING_THEORY_CLOSURE_V001.md` with its hostile audit. It is an exact
   implication under adopted RGRL and WTC-H1--H5, with its empirical, microscopic, strict-origin,
   and numerical-`G` ceilings attached.
2. **Finite relational accumulation.** The computed L4--L14 ladder is a parallel microscopic and
   finite-size test. Exact L12 custody and the Stage-6 decision are closed; the current L14
   target/hostile outcome is pending. This route is not silently inserted as a premise of the
   record-first theorem.

The full Git history preserves the earlier direct upward-construction program, its obstructions,
and the pivot to the finite ladder. The curated Zenodo packet follows the evidence needed to check
the current proof path.

## Current authority order

When two narratives differ, use this order:

1. immutable target/hostile evidence and adjudication records;
2. source ledgers under `ledger/`;
3. executable certificate modules and their validators under `model/`;
4. `MODEL.md` and `GLOSSARY.md`;
5. generated views: `STATUS_LEDGER_V001.md`, `THE_CLAIMS_V001.md`,
   `THE_PLAN_V001.md`, and `PROOF_V002.md`;
6. the repository-facing `PROOF_GUIDE.md`;
7. historical narratives, schedules, and handoffs, which remain provenance and must say when they
   have been superseded.

The URM records status; it does not upgrade a claim merely because the claim is added to the model.
The ledger's special `PROVED` status remains stricter than “exact theorem” or “exact finite
computation.”

## Finding-to-model process

For each new result:

1. authenticate the declared source, target output, hostile output, and adjudication;
2. state the narrowest supported classification and its refuter;
3. register or update the source ledger and plan task without deleting historical rows;
4. add a zero-input, hash-pinned executable certificate when the result changes live model state;
5. add a fail-closed validator that checks hashes, schemas, values, claim ceilings, and tamper
   refusal;
6. update `MODEL.md` and `GLOSSARY.md` only at the level of changed meaning;
7. regenerate the ledger and proof views from their sources;
8. update the proof guide and Zenodo allowlist by exact path;
9. run focused gates, the proof gate, capsule checks, and the aggregate URM comparison;
10. record inherited aggregate failures explicitly rather than advertising a partial green result.

## Alpha typing

The historical generic perturbation magnitude and physical fine-structure alpha are separate
objects.

- `ALLOW`: finite record formation admits a non-singleton set of alpha-bearing active-EM parents.
- `REQUIRE`: after the actual visible compact-`U(1)` host is fixed, same-sector records inherit its
  empirically anchored coupling and RG trajectory.
- `SELECT`: why that parent or numerical value is actual remains separate and may have no dynamical
  selector.

This is not a numerical derivation of `1/137`. The remaining T-38 barrier-dependence question is
also separate; it does not reopen same-sector inheritance.

## Finite progression and L14 ingestion

The current table is:

| scale | strict common-lineage support | status |
|---|---:|---|
| L08 | `0.0244800482` | authenticated-target-cache diagnostic |
| L10 | `0.0687369678` | authenticated-target-cache diagnostic |
| L12 target | `0.11570852222694002` | authenticated reconstruction |
| L12 hostile | `0.11570852222694036` | independent hostile reconstruction |
| L12 conservative | `0.11570852222694002` | governing finite diagnostic |
| L14 | `PENDING_AUTHENTICATED_RESULT` | target/hostile adjudication pending |

The finite values are strictly increasing under the declared extraction. No fit or extrapolation
theorem is attached.

The L14 seam contains two decisions, not one. The owner-once Phase-1 merge first authenticates both
branches and original numerical predicates, checks the literal q5 and physical q6-core witnesses,
checks contiguous coverage of `[3/16,11/48)`, and applies the optimistic reachability bound formed
from Phase-1 passing mass plus unopened q8/q9 mass. That Phase-2 decision can authorize Phase 3 q8/q9
only. After authenticated Phase-3 target/hostile evidence exists, the distinct final finite-scout
gate retains the lower independently computed branch mass for each actually passing sector,
deduplicates q identity, sums that conservative mass, and tests `>= 0.50`. Only the complete chain may
replace the pending L14 row. The merge must preserve the complete declared q-domain. Operational
completion is not scientific completion; nonconvergence or missing authentication is an obstruction,
not a scientific rejection. Runtime and cost thresholds are notices unless a declared physics
predicate requires a stop. Neither decision is an exact or asymptotic `z=1` emergence theorem.

The recovery also makes provenance branch-specific: the Hostile branch remains on its original R5
release while Target uses the authorized fresh R6 restart. The final public record must bind each
branch to its own source manifest, kernel dependencies, runtime, and restart history. Matching
top-level kernel hashes do not make R5 and R6 interchangeable, because execution dependencies differ.

After authentication, the same merge record must drive all of the following updates:

- the L14 row, Phase-2 reachability-authorization decision, authenticated Phase-3 q8/q9 evidence,
  and separate final finite-scout decision;
- `model/relational_accumulation.py` and its validator;
- `MODEL.md`, the status/claims ledgers, and the generated proof;
- `PROOF_GUIDE.md`;
- the Zenodo result/provenance inputs;
- the final Git release manifest and release notes.

No one of those surfaces may be filled from a screenshot, branch-local file, estimated trajectory,
or infrastructure state.

## Release process

Preparation does not authorize publication. The order is:

1. close and authenticate L14;
2. finish URM reconciliation and compare aggregate validation with the dated baseline;
3. review explicit Git path allowlists and commit the preserved scientific-history sequence;
4. verify the `gravacle/where-atoms-come-from` destination and push only with authorization;
5. bind an immutable release tag;
6. build and verify the curated capsule from its version-controlled manifest;
7. supply license, authorship/ORCID, citation, release date/version, and DOI metadata;
8. deposit to Zenodo and write the exact version DOI and concept DOI back into the repository.

The Git repository remains the full research journey. Zenodo remains the compact, independently
checkable proof and reproduction path.
