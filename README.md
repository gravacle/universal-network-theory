# Universal Network Theory

This repository is the working scientific record for **Universal Network
Theory (UNT)**, its Record Formation Theory and Gravity Formation Theory
branches, and the **Universal Record Model (URM)** executable custody surface.
It preserves proofs, executable certificates, numerical calculations,
independent or hostile audits, failed routes, repairs, and the append-only
history needed to distinguish those categories.

## Current result

The governing result is the
[Universal Network Theory Closure Theorem](UNIVERSAL_NETWORK_THEORY_CLOSURE_THEOREM_V001.md),
which composes the completed Record Formation Theory and Gravity Formation
Theory results while preserving every component premise and proof type. The
governing finite formation result inside that theorem is that the authenticated `A009--A016` record
block passes the **finite ARGER Gate**, yielding the finite GFT `z=1`
classification. The Gate is applied at ARGER's `GATE` step. The finite
record block is the evidence domain evaluated at that step; it is not part of
ARGER's identity, which remains the full `ALLOW -> REQUIRE -> GATE -> EM ->
RECORD` process. The Gate requires:

1. exact bounded record membership for every selected sector;
2. deduplicated authenticated probability mass greater than `0.50`; and
3. strictly positive authenticated finite probe visibility in every unique
   selected sector.

Here `L` is the even size and one-pass horizon of an independently constructed
owner-once periodic-prism parent: `L` active cells, `2L` carrier sites, `3L`
transport edges, and `L` fresh admissions before revisit. Thus `L = 4` through
`L = 12` are sizes in one finite model family, not proof levels or stages of
one growing run. The exact `L = 4` through `L = 12` theorem and its
independent hostile audit establish bounded
record membership. The authenticated masses are:

| L | selected sectors | block mass |
|---:|:---|---:|
| 4 | `q1--q2` | `0.7260206189754993` |
| 6 | `q2--q3` | `0.5846615608350367` |
| 8 | `q2--q4` | `0.7373965730354166` |
| 10 | `q3--q5` | `0.6500987927669427` |
| 12 | `q4--q6` | `0.56956498393327842` |

All five masses exceed one half. Together, these authenticated sizes constitute
the required finite evidence domain through `L = 12`. The 40 atom/size assignments reduce to 13
unique `(L,q)` rows across the five independently constructed domains, all
with positive finite probe visibility. Their global
minimum is

```text
R_low = 0.4280947078156539 > 0.
```

Under the adopted finite ARGER Gate, the completed construction through `L = 12`
establishes the UNT **finite GFT `z=1` classification**. `L = 12` completes the
authenticated evidence obligation for this Gate application. Here, **finite** identifies the theorem's
authenticated domain; it does not mark the conclusion as provisional, and no
`L -> infinity` limit is part of the declared finite classification.

## Universal Network Theory proof map

The publication-facing proof guide presents the UNT closure theorem together
with its completed proofs, authenticated finite results, and explicitly typed
conditional components:

| Result | Completed scope |
|---|---|
| `UNT-CLOSURE-V001` — UNIVERSAL NETWORK THEORY CLOSURE | Exact typed composition of universal RFT coverage, the domain alpha requirement, finite GFT `z=1`, record-conditioned geometry, and the conditional macroscopic Einstein response |
| `L = 4` intrinsic-admission theorem | Exact finite one-pass blocked-null, nonnegative-write, and conservation/accounting theorem; target `10/10`, independent reconstruction `12/12`, hostile verifier `50/50` |
| `G-C` — THE RECORD LAWS | Exact formal lifetime, steady-value, and surviving-record theorem family within the declared model |
| Alpha `ALLOW`/`REQUIRE` | Exact construction interval plus exact conditional governing-domain inheritance theorem |
| `G-G` — GRAVITY FORMATION THEORY | Certified finite GFT `z=1` classification under the ARGER Gate plus separately conditional infrared theorem |
| `G-H` — FINITE RELATIONAL ACCUMULATION | Exact/audited `L = 4` through `L = 12` block membership, mass, and visibility evidence consumed by the Gate |
| `G-I` — FOUNDATIONAL RECORD CLOSURE | Exact AURFT Coverage-U closure conditional on adopted U-DCL |

Definitions, superseded routes, URM integration, and publication
administration remain documented separately from the proof table.

See the [Universal Network Theory Major Proof Index](publication/zenodo_reproduction_v001/UNIVERSAL_NETWORK_THEORY_MAJOR_PROOF_INDEX.md)
for the controlling theorem, independent audit, executable check, external
citations, and explicit ceiling for every listed result.
The [Word edition](publication/zenodo_reproduction_v001/UNIVERSAL_NETWORK_THEORY_MAJOR_PROOF_INDEX.docx)
contains the same consolidated guide for review and publication workflows.

## Proof boundaries

- **Finite GFT `z=1`:** adopted and established by the complete authenticated
  construction through L12.
- **Dynamical exponent `z=1`:** supported by the separate conditional
  same-model LL-P theorem on the declared density interval. This is a
  supplementary result, not an unfinished obligation of the finite Gate.
- **Alpha:** the declared active-EM construction proves an exact `ALLOW`
  interval. Within a governing compact-`U(1)` domain satisfying `SAI1--SAI8`,
  the measured alpha and its RG trajectory are constitutive `REQUIRE` data for
  every coefficient-preserving same-sector record. `SELECT` is the separately
  typed question of governing-domain selection.
- **L12 majority margin:** `0.06956498393327842` is exact arithmetic, but its
  interpretation as an alpha write cost, binding energy, mass defect, or
  spectral residue remains a hypothesis.

## Read first

1. [Universal Network Theory Closure Theorem](UNIVERSAL_NETWORK_THEORY_CLOSURE_THEOREM_V001.md)
2. [Universal Network Theory Major Proof Index](publication/zenodo_reproduction_v001/UNIVERSAL_NETWORK_THEORY_MAJOR_PROOF_INDEX.md)
3. [Universal Network Theory architecture](UNIVERSAL_NETWORK_THEORY_ARCHITECTURE_V001.md)
4. [Finite ARGER Gate adoption](ARGER_GATE_ADOPTION_2026-09-16.md)
5. [Exact L4--L12 bounded record theorem](L4_L12_EXTENDIBLE_RECORD_BLOCK_THEOREM_2026-09-16.md)
6. [Independent hostile audit](AUDIT_L4_L12_EXTENDIBLE_RECORD_BLOCK_THEOREM_2026-09-16.md)
7. [Native ARGER Gate evidence](DEVELOPMENT_R_ARGER_GATE_V001/README.md)
8. [Conditional same-model dynamical z=1 theorem](DEVELOPMENT_R_L12_Z1_LUTTINGER_CROSSCHECK_V001/CONDITIONAL_Z1_BRIDGE_THEOREM.md)
9. [URM model](MODEL.md) and [glossary](GLOSSARY.md)

## Reproduction and release status

The full Git history preserves the scientific journey. The bounded Zenodo
packet under `publication/zenodo_reproduction_v001/` is intended to carry the
curated proof path, exact source, compact authenticated evidence, claim map,
and cryptographic manifest without bulk/full raw caches, transient cloud state,
or credentials. Only compact, allowlisted authenticated L08/L10 reproduction
slices and finite-sector evidence are retained.

This checkout contains substantial work beyond the currently published GitHub
`main` branch. It is release-preparation material, not yet a tagged or deposited
scientific release. No commit, push, tag, Zenodo publication, or DOI deposit is
authorized merely by this README.

The root `README.rst` is retained as a historical pre-adoption snapshot. This
`README.md` is the current release-facing status surface.

> **Historical note.** An attempted L14 extension ended before an authenticated
> result and contributes no evidence or premise to the completed L4--L12 proof.
> Its disposition remains available in the chronological Git record.
