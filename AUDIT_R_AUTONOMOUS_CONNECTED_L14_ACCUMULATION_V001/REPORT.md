# Hostile audit report — L14 accumulation V001

## Verdict

**PASS for the bounded conditional microscopic numerical record.** The target
is eligible for promotion only with its existing claim ceilings. This audit
does not promote an accumulation axis, autonomous support, or any
macroscopic interpretation.

The prior verdict was correctly fail-closed because it had not executed an
independent L14 evolution. That history remains part of custody. The rejected
full-word-current and Python-object-transition lifts were not run. The present
remedy is a new implementation and imports no target or engine function.

## Independent construction

The audit generates the finite prism relabeling group from two independently
written prism-coordinate generators. A compiled generator BFS assigns every
one of the `2^28 = 268,435,456` words to an orbit. It obtains exactly
`9,608,050` orbits and histogram
`{1:4, 2:6, 7:252, 14:41742, 28:9566046}`.

Each representative's 42 owner-once swaps is aggregated by quotient
destination in a fixed 64-slot local table. The retained CSR has `201,326,276`
entries and `3,096,758,548` bytes across 64-bit offsets, 32-bit destinations,
64-bit Hamiltonian coefficients, byte multiplicities, and two signed byte
current weights. There is no Python object storage proportional to
transitions. The full-word orbit map is released before evolution. Diagonal
observables use orbit representatives only.

Before L14, an explicit L4 full-word calculation attacks the quotient formulas
on a deterministic random invariant state. Full-space versus quotient errors
are `6.245e-17` for the Hamiltonian action, `1.735e-17` across all oriented
currents, and `1.110e-16` for the diagonal occupation reduction. The L14
bilinear Hermiticity probe is `8.179e-17`; source current and source norm error
are zero.

## Independent numerical reconstruction

A classical RK4 integrator, distinct from the target's order-10 Taylor
integrator, was run at 1,024 and 2,048 panels. Currents were integrated by
composite Simpson quadrature on the RK4 nodes. Fine/coarse differences are
`2.271e-10` for state amplitudes, `1.008e-9` for occupations, and `2.527e-10`
for oriented integrated currents.

The fine result gives:

- alternating occupations `0.26064734391335903` and
  `0.23935265608383544`;
- internal current magnitude `0.08828506936513429` on all 28 internal
  supports and connector magnitude `0.08407720519894114` on all 14 connector
  supports, with the independently reconstructed alternating orientations;
- expected retained total `685.9999999961508`;
- global total throughput `357.6081558708757` and connector throughput
  `115.35392553294726`;
- maximum absolute connected-edge correlation `0.03604917681459584`;
- raw unassigned owner-edge residual `4.0454239957909977e-10` L1 and
  `1.585070963372459e-11` Linf per component.

The raw residual is not called a defect. Its global L1 sum bound is
`3.9645155158751776e-08`.

## Target attack

Against the sealed target, the independent differences are:

| Quantity | Absolute/Linf difference |
|---|---:|
| all 28 occupations | `6.584e-11` |
| all 42 oriented currents | `1.897e-11` |
| maximum correlation metric | `3.083e-12` |
| retained total | `3.800e-9` |
| global total throughput | `7.729e-8` |
| global connector throughput | `2.523e-8` |
| all total/connector comparator fields | `9.712e-9` |
| raw residual L1 norm | `2.274e-10` |
| raw residual Linf norm | `9.505e-12` |

The two residual-norm differences in this table were appended arithmetically
from the sealed successful independent result and sealed target; they did not
require or purport to be another evolution run.

The explicit owner-edge balance was independently formed from the 42
oriented currents; no target residual or incidence product was substituted.
The independently recomputed L14/L4, L14/L6, L14/L8, L14/L10, and L14/L12
total, per-retained, connector, and connector-per-retained comparators all
pass the stated tolerance.

Norm error is `5.086e-12`; energy error is zero at reported precision; the
maximum number-sector-law change is `1.202e-12`. The run completed in
`1299.514401041 s` with `5,746,900,992` bytes (`5.3522186279296875 GiB`)
maximum RSS under the 40 GiB audit guard on the declared 48 GiB environment.
Runtime and RSS are one-run engineering observations only.

## Claim classification and ceiling

**Proved:** independent finite group generation, complete generator-BFS orbit
assignment, orbit census, fixed-width aggregated quotient construction, and
the L4 full-space operator/current/diagonal equivalence checks.

**Adopted:** inherited F3-MDC `alpha=r0`.

**Conditional:** connected support, source family, `kappa`, content, routing,
clock, complete read, and the chosen finite accumulation slice.

**Empirical/numerical:** RK4 evolution, currents, occupations, correlations,
retained total, raw residual, finite comparators, runtime, and RSS.

**Open:** the next accumulation-axis choice; autonomous support; and every
macroscopic response question.

This is one finite microscopic record. It does not establish monotonicity,
convergence, a limit, fit, exponent, scaling law, or critical behavior. It
inserts no physical grid, continuum assumption, Ward axiom, phase, graviton,
or gravity.
