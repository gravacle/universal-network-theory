# UNT foundational premise-discharge recovery V001

**Opened:** 2026-09-25
**Status:** active read-only Phase A recovery; finite lineage updates reconciled 2026-09-25
**Governing program:** `../UNT_FOUNDATIONAL_PREMISE_DISCHARGE_PROGRAM_V001.md`

This directory turns the premise-discharge program into auditable working
artifacts. It does not change a sealed historical result, promote a premise, or
declare a new physics result.

## Parallel-execution boundary

Phase A began beside the then-active owner-once L10/L12 calculation. Both
atomic branches are now complete and reconciled, so Phase A may read those
final artifacts, Git metadata, release manifests, and the finished Zenodo ZIP.
It must not:

- open, summarize, or infer any partial numerical artifact from a future live
  calculation;
- modify, stop, restart, reprioritize, or otherwise disturb the live workers;
- launch a new expensive numerical calculation;
- alter sealed historical bytes; or
- publish, tag, merge, or replace the current Zenodo record.

## Deliverables

| Artifact | Purpose | Current state |
|---|---|---|
| `FOUNDATIONAL_CLAIM_LEDGER.tsv` | One evidence class and current ceiling for each load-bearing claim | 41-row load-bearing spine: 30 adjudicated or explicitly open/custody-limited rows; 11 seeded rows still awaiting full adjudication |
| `FOUNDATIONAL_DEPENDENCY_DAG.md` | Separates proved/computed nodes from adopted and open bridge nodes | Critical-path map and D1--D3/E1--E4 status matrix complete |
| `ZENODO_FOUNDATIONAL_COVERAGE_GAP.md` | Compares the finite reproduction capsule with the broader gravity dependency chain | ZIP/manifest integrity and load-bearing dependency crosswalk complete; full historical row expansion remains in progress |

## Execution plan and effort

1. **Claim genealogy and source anchoring — 2 to 4 focused hours for the
   load-bearing spine; 24 to 48 analyst hours for the full historical
   repository.** Recover exact wording, date, bytes, premise set, audits,
   counterevidence, successor, and present ceiling.
2. **Premise-discharge crosswalk — 2 to 3 focused hours for the current
   RGRL/WTC spine; 16 to 32 analyst hours for full re-adjudication.** Mark each
   item `DEFINITION`, `ADOPTED`, `EXACT_DERIVED`, `COMPUTED`, `EMPIRICAL`,
   `NEGATIVE`, `SUPERSEDED`, or `OPEN`.
3. **Zenodo coverage audit — 1 to 2 focused hours for the load-bearing spine;
   8 to 16 analyst hours after the full genealogy.** Distinguish capsule
   reproduction scope from full-theory proof custody.
4. **Hostile re-adjudication — 2 to 4 focused hours after the spine is
   assembled.** Search for circular identifications, theorem antecedents that
   were treated as conclusions, duplicated claims, and superseded routes.
5. **Correction packet — 2 to 5 focused hours after adjudication.** Update only
   current, unsealed theory/URM/publication surfaces, preserving positive,
   negative, and inconclusive outcomes identically.

The research stages after recovery—native relation selection, finite curvature,
continuum refinement, RGRL discharge, WTC discharge, and independent physical
prediction—are deliberately not assigned engineering-style hour estimates.
They require new theorems, counterexamples, or experiments.

## Ledger semantics and hostile QA

The ledger separates `primary_class`, `depends_on_adopted_or_open`, and
`domain_scope`. A finite domain restriction is therefore not mislabeled as
dependence on an adopted or open physical antecedent. Source fields repeat the
path for every cited line range so the anchors are unambiguous.

Two independent read-only hostile passes have checked the load-bearing spine.
They confirmed that D1--D3 and E1--E4 remain open and found no finite or
conditional theorem promoted to a completed physical discharge. Their concrete
corrections are now incorporated: H9/H14 are separated; PMICS kinematics and
its RGRL-dependent physical reading are separated; GL6BQ's negative and
conditional-design claims are separated and kept on custody hold; stale audit
metadata for TROV, SDCP, GL6T, and GL6BC is repaired; and the dependency DAG no
longer makes graph curvature a universal prerequisite for independent D1/D2
routes.

## Completion rule

Phase A is complete only when every publication-level gravity statement has a
path through the dependency DAG and every load-bearing node has a source,
evidence class, present ceiling, and public-package status. An unresolved or
adopted node remains visible; it is never closed by prose.
