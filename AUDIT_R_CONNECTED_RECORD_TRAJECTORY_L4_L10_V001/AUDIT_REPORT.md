# Hostile audit — connected record trajectory L4--L10

The compilation passes without correction. An independent direct reader pins the four sealed input hashes, explicitly selects the L4 `kappa=pi/2` row, and reconstructs every output row and adjacent comparator. All raw fields match the target within floating evaluation.

The exact finite census identities hold at every enumerated size: sites `L^3`, lineages `L^3/2`, components `L^2/2`, owner-once edges `3L^3/2`, and analytic retained expectation `L^3/4`. These identities describe the supplied finite construction, not scaling or continuum behavior.

All throughput, connector share, current magnitude, occupation, correlation, residual, and adjacent-comparator tables reproduce the four sealed records. In particular the active L4 residual—not another L4 row—is `(1.3600232051658168e-15, 3.3306690738754696e-16)` per component.

The documented float-equality repair is technically justified: strict binary equality fails for all three retained/site ratio comparisons, by `1.66e-13`, `-3.86e-14`, and `-4.22e-14`, while the stated `1e-12` tolerance passes. The deleted failed candidate is author-recorded pre-promotion custody, not independently recoverable scientific evidence; the current compiler changes only this comparison tolerance.

The sequence is finite and nonmonotone in several normalized observables. Nothing here proves monotonicity, convergence, a limit, exponent, fitted scaling, autonomous support or geometry, grid, continuum, Ward identity, critical or generic phase, graviton, or gravity. Residuals remain numerical unassigned record-ledger terms, not defects. No correction required.
