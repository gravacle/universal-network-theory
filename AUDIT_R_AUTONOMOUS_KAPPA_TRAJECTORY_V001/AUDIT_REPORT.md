# Hostile audit — autonomous BS09 kappa trajectory

**PASS_CONDITIONAL_AUTONOMOUS_KAPPA_MAP.** An independent simultaneous-Hamiltonian reconstruction covers L4/L6/L8 at all ten declared `kappa` values, 30 rows total. It reproduces occupations, correctly oriented integrated edge currents, continuity residuals, absolute throughput, connected correlations, retained and number laws, norm and energy controls, and L8/L4 raw ratios.

The current convention passes the discrete identity `q(kappa)-q(0)+J_out-J_in=0`. The `kappa=0` currents vanish; the `pi/2` rows have every cycle support active and reproduce the earlier hostile-audited autonomous L4/L8 packet.

Algebraically, literal BS09 is `epsilon Q-tA`. Since `[Q,A]=0`, and the prepared state lies in number sectors while occupation, current and number-law observables commute with total `Q`, the uniform onsite evolution contributes only sector phases that cancel in these expectations. Rescaling the current integral by `s=t tau'/hbar` leaves dependence on `kappa=t tau/hbar`; this does not select `t`, `tau`, or a clock.

Support, sampled kappa values, separate coefficient/clock calibration, content sector and complete read remain conditional. There is no finite gate order or node stagger. Residuals are unassigned rather than defects. No coefficient selection, physical grid, continuum, Ward, or gravity claim is promoted. No correction is required before promotion.
