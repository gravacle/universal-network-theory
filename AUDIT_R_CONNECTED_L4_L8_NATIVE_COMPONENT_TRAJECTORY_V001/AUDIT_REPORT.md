# Hostile audit — native-component L4/L8 trajectory

**PASS_CONTROLLED_NUMERICAL_CONNECTED_FINITE_SIZE_TRAJECTORY.** Independent reconstructions used no target import. L4 partitions 32 heads into 4 eight-site components; L8 partitions 256 heads into 16 sixteen-site components. Native support counts are 12 and 24, with 4/4 and 8/8 rungs active. Owner-once counts are 36 and 72 events per component.

Retained totals, event traffic, cumulative-net traffic, inter-cycle traffic, correlations, ledgers, norms, number laws, all raw total ratios, and all raw per-head ratios reproduce the target. The L8 row reproduces the prior audited reversed-preparation, forward-order depth-3 circuit.

The cell/head ratio of eight does not force any observable ratio: the three traffic-total ratios differ from eight, and their per-head ratios differ from one. These are two-size raw ratios only, not an exponent, limit, scaling law, or generic law. Schedule and preparation remain explicit selected-history labels. The read is complete failure-inclusive and attachment is inherited adopted `alpha=r0`. Coordinates are finite enumeration, not a physical grid; residuals are unassigned, not defects. No continuum, critical, Ward, or gravity claim is promoted. No material defect or repair was found.
