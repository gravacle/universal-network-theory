# Hostile audit — connected L8 preparation-pattern screen V001

## Disposition

**PASS_CONTROLLED_NUMERICAL_PREPARATION_SCREEN.** A fresh 65,536-state reconstruction holds the 24-support forward order, depth 3, and terminal instrument fixed while applying the explicitly labeled `same`, `reversed`, `first_only`, and `second_only` onsite weights. The census is 72 owner-once events per pattern, 288 total, with six full-state pairwise comparisons.

The `same` pattern has zero active rungs within tolerance but retains all 16 ring supports and nonzero internal traffic. `reversed`, `first_only`, and `second_only` each activate all 24 cumulative supports, including 8/8 rungs. Retention, gate signs, record ledgers, norm, and record-number law pass. Full terminal-distribution TV, state fidelity, and occupation-L1 reproduce the target pattern dependence.

The one-sided cases are not merely close in summaries: after the exact permutation exchanging the low and high eight basis bits, their complete terminal probability distributions agree within floating tolerance, and their 16 occupations agree after the same label swap. The `reversed` run independently reproduces the prior audited forward depth-3 circuit.

## Narrow conclusion and custody

Preparation asymmetry is necessary for active rung transport only in this fixed symmetric circuit. No generic necessity, preferred preparation, pattern measure, averaging rule, or selection law follows. Pattern labels remain explicit selected-parent data.

The read is the complete failure-inclusive product PVM and attachment is inherited adopted `alpha=r0`, not bare-F3 derived. Tuple labels are finite enumeration, not a physical grid. Raw residuals remain unassigned, not defects or exact zeros. No continuum, critical/scaling, Ward, macroscopic, or gravity claim is promoted. No material defect or repair was found.
