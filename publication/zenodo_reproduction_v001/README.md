# Universal Network Theory: finite relational reproduction capsule (`L = 4` through `L = 12`)

This directory prepares a curated, deterministic Zenodo capsule. It does not
publish, create a DOI, push Git, or run paid cloud computation.

The release core is the authenticated `L = 4` through `L = 12` finite
relational evidence path, the exact
finite record-envelope theorem and internal hostile audit, and the adopted UNT finite
ARGER Gate. The Gate evaluates the finite record envelope as its
evidence domain at ARGER's `GATE` step; the block is not part of ARGER's
identity as the full `ALLOW -> REQUIRE -> GATE -> EM -> RECORD` process. The separately typed conditional
dynamical-`z=1` theorem packet is also preserved.

Here `L` is the even size and one-pass horizon of an independently constructed
owner-once periodic-prism domain: `L` active cells, `2L` carrier sites, `3L`
transport edges, and `L` fresh admissions before revisit. Values such as
`L = 8`, `L = 10`, and `L = 12` are therefore successively larger members of
one finite model family, not proof levels, software versions, or stages of one
growing run. ARGER is UNT's ordered dependency sequence for the formation roles
of whichever actual physical record is supplied by parent dynamics; `GATE` is
its single Gate stage.

## What this capsule establishes

The exact structural theorem proves the declared bounded record roles for the
selected `A009--A016` envelope at `L = 4, 6, 8, 10, 12`. Its internal hostile
audit passes. Deduplicating repeated atom assignments gives:

| L | selected sectors | authenticated block mass |
|---:|:---|---:|
| 4 | `q1--q2` | `0.7260206189754993` |
| 6 | `q2--q3` | `0.5846615608350367` |
| 8 | `q2--q4` | `0.7373965730354166` |
| 10 | `q3--q5` | `0.6500987927669427` |
| 12 | `q4--q6` | `0.56956498393327842` |

Every value is greater than `0.50`. The 13 unique selected `(L,q)` rows span
the five independently constructed finite domains; they are not 13 sectors in
one `L = 12` system. Across those rows,
the minimum authenticated finite probe visibility is
`R_low = 0.4280947078156539 > 0`. L4--L8 use complete/sealed finite-sector
diagonalization; L10/L12 use separately implemented Target/Blind reconstructions.

The project has adopted the finite ARGER Gate as its governing project-native
rule. Under that rule, exact bounded membership,
deduplicated mass greater than one half, and positive finite visibility imply
the UNT finite GFT `z=1` classification. All three premises are met at every
authenticated size from `L = 4` through `L = 12`.

The conditional macroscopic response is carried separately by adopted RGRL and
`WTC-H1--H5`; the finite Gate pass does not establish those premises.

A separately sealed theorem gives physical dynamical exponent `z=1` on its
declared density interval conditional on the pinned LL-P premise. That theorem
is a supplementary physical result, not an unfinished component of the finite
evidence obligation for the Gate application completed through `L = 12`.

The Alpha result is likewise explicitly typed. Across controlled active-EM
domains, ARGER's `ALLOW` stage admits the exact construction-scoped interval
stated in the Universal Network Theory Major Proof Index. Conditional on
independently established ancestry to a governing compact-`U(1)` domain
satisfying `SAI1--SAI8`, every coefficient-preserving same-sector record
inherits that domain's measured alpha and RG trajectory as `REQUIRE` data.
`SELECT` is the separately typed question of governing-domain selection.

The capsule also recognizes the completed owner-once joint lineage--carrier
witness calculation at `L = 10` and `L = 12`. Independently implemented exact
finite reconstruction branches produced:

| L | Target `D_L` | Hostile `D_L` | capacity bound `B_L` | Target `eta_L = D_L/B_L` |
|---:|---:|---:|---:|---:|
| 10 | `-0.0003118488593728413` | `-0.00031184885937283205` | `0.22714282661304128` | `-0.0013729196912042685` |
| 12 | `-0.00003236588485223031` | `-0.000032365884852249583` | `0.23119534505251413` | `-0.0001399936700493631` |

The raw two-sided diagnostic is `T_10:12 = 32365.88485223031`, establishing a
numerically resolved nonzero finite association through `L = 12`. The frozen
capacity normalization gives magnitudes of about `0.137%` of sector capacity
at `L = 10` and `0.0140%` at `L = 12`; this scale report is not an additional
pass/fail condition. The frozen
concurrent schedule required releases within 60 seconds, but the observed
launch skew was 10,748 seconds. This timing condition was a procedural
scheduling and custody guard; because it was missed, the release does not call
the result a pristine strict-protocol held-out pass. It discloses the deviation
while preserving the deterministic agreement. The separately frozen signed
no-decline floor fails: the witness changes sign after `L = 8` and its magnitude
declines substantially by `L = 12`. No all-`L`, Gate, RGRL/WTC, alpha, geometry,
or gravity conclusion is promoted from this result. See the
[compact recognition record](evidence/owner_once_joint_lineage_carrier/OWNER_ONCE_JOINT_LINEAGE_CARRIER_L10_L12_RESULT.json).

Two further bounded record diagnostics are recognized without promotion beyond
their executed domains:

- **L8 lineage-order Ollivier--Ricci controlled null.** On the frozen lineage
  formation-order path, the Ollivier--Ricci statistic is
  `rho = 0.1543033499620919` with exact
  one-sided `p_plus = 0.3619047619047619`. The classification is
  `NO_RESOLVED_LINEAGE_ORDER_CURVATURE_RECORD_ASSOCIATION_L8`. This is a
  controlled null on one schedule-derived path, not spacetime curvature and
  not evidence for geometry or gravity. The extracted verifier authenticates
  the compact recognition record and the sealed result, freeze, protocol, and
  manifest, but the capsule excludes the bulk raw L8 replay arrays.
- **L4 autonomous lineage-sensitive continuation.** At one fixed L4
  checkpoint, the independently reconstructed lineage-sensitive revisit gives
  `Delta_C = 0.14761185701903007` and occupation-profile
  `RMS = 0.006009875541368592`, with zero captured warnings and maximum
  Target/audit difference `1.1102230246251565e-16`. This establishes one
  finite mechanism at L4 only; it does not establish scaling, curvature,
  geometry, RGRL/WTC, alpha, or gravity. The extracted verifier authenticates
  the compact recognition record, Target result, freeze and protocol, and
  independent warning-audited result.

Start with:

- [Claude Code review guide](CLAUDE.md) -- the read-only operating contract,
  verification entry point, governing terminology, and review boundaries for
  an automated code-and-evidence review
- [Universal Network Theory Closure Theorem](UNIVERSAL_NETWORK_THEORY_CLOSURE_THEOREM_V001.md) -- the exact type-preserving master composition of the bounded and conditional RFT and GFT results
- [Word edition of the UNT Closure Theorem](UNIVERSAL_NETWORK_THEORY_CLOSURE_THEOREM_V001.docx) -- the publication-ready theorem document
- [Universal Network Theory Major Proof Index](UNIVERSAL_NETWORK_THEORY_MAJOR_PROOF_INDEX.md) -- the consolidated guide to completed proofs and certified results, their premises, dependency order, external citations, separately documented program boundaries, reproduction, and capsule-versus-Git custody
- [Word edition of the Major Proof Index](UNIVERSAL_NETWORK_THEORY_MAJOR_PROOF_INDEX.docx) -- the same consolidated guide in publication-ready `.docx` form
- [Completed-result claim map](PUBLICATION_CLAIM_MAP.tsv) -- release statements limited to completed exact or certified results and explicit conditional theorems with their premises; definitions, open programs, history, and release administration are documented elsewhere rather than presented as proof claims
- [ARGER Gate adoption record](ARGER_GATE_ADOPTION_2026-09-16.md)
- [Exact L4--L12 record-envelope theorem](L4_L12_EXTENDIBLE_RECORD_BLOCK_THEOREM_2026-09-16.md)
- [Internal hostile audit](AUDIT_L4_L12_EXTENDIBLE_RECORD_BLOCK_THEOREM_2026-09-16.md)
- [Native ARGER Gate evidence reconstruction](DEVELOPMENT_R_ARGER_GATE_V001/README.md)
- [Conditional dynamical-z=1 theorem](DEVELOPMENT_R_L12_Z1_LUTTINGER_CROSSCHECK_V001/CONDITIONAL_Z1_BRIDGE_THEOREM.md)

The Markdown theorem and Major Proof Index are the content authorities. Their
Word editions were generated during release preparation by the historical helper
`publication/zenodo_reproduction_v001/tools/build_major_proof_index_docx.py`;
both formats of each document are included in the capsule.

## Reproduce the bounded checks

The verifier uses the host's Python 3 interpreter and NumPy installation. The
capsule deliberately does not embed a platform-specific interpreter or wheel.

From an extracted capsule root:

```bash
python3 -B tools/verify_extracted_capsule.py .
```

The extracted verifier checks archive-local navigation, exact hashes for the
record theorem, internal hostile audit, and sealed z1 packet, reruns the native ARGER
reconstruction, verifies the L08/L10 cache extraction, authenticates the
full-workflow L12 strict-lineage result and custody, authenticates the proof-packet layouts,
executes the portable alpha scope gate and algebraic witness/DAG regression,
authenticates the two independent owner-once joint lineage--carrier raw results
and their disclosed timing and signed-persistence dispositions, authenticates
the compact and sealed L8 lineage-order controlled-null records without the
excluded bulk replay arrays, authenticates the compact, Target,
freeze/protocol, and independent-audit L4 continuation records, and runs the
focused relational URM validator against an authenticated public projection of
its exact repository dependency closure. The only projected
positions are the two path-neutral L08/L10 cache manifests and the adapter
that authenticates them back to their raw source-manifest hashes; the archive
also retains and authenticates the byte-exact relational model source. The
repository Alpha URM integration result is documentary because its complete
aggregate runner is outside this focused closure and retained only in the
historical development record. The verifier does not execute excluded
cloud-run history or advertise broader validators whose complete dependency
closures are not in the capsule.

For the secondary strict common-lineage diagnostic, the capsule recomputes
`L = 8` and `L = 10`. The `L = 12` Target/Hostile values are authenticated
outputs of the full repository workflow. A 2026-09-18 review rerun reproduced
both values and generated a report byte-identical to the sealed report. The
complete audited workspace is approximately 59 GB; the minimum runnable
`L = 12` reconstruction set alone is 13,485,786,130 bytes (12.559617 GiB).
This focused archive preserves the method, result, and cryptographic custody
without duplicating those bulk generated input arrays.

The native ARGER Gate evidence validator can also be run directly:

```bash
python3 -B DEVELOPMENT_R_ARGER_GATE_V001/arger_gate.py
python3 -B DEVELOPMENT_R_ARGER_GATE_V001/test_arger_gate.py
```

From the repository root, preparation checks are:

```bash
python3 -B publication/zenodo_reproduction_v001/build_capsule.py check
python3 -B -m unittest discover -s publication/zenodo_reproduction_v001 -p 'test_*.py'
```

Those two preparation commands are for a source-repository checkout. From an
extracted capsule, use `python3 -B tools/verify_extracted_capsule.py .`; do not
run blanket test discovery against repository-only test modules whose source
layout is intentionally absent.

The builder selects only explicit manifest paths, rejects unsafe archive
paths, verifies hash-pinned closures, excludes private path-bearing research
envelopes, and builds a deterministic archive only when explicitly asked.

## Release boundary

Preparation may proceed without final publication metadata. A release build
still requires human-supplied values for the originating Git tag and commit as
historical provenance, together with the version, release date, and Zenodo DOI
metadata. Brian Mulconrey's validated
ORCID and the approved Apache-2.0 software and CC-BY-4.0 research-content
licenses are pinned directly in the release metadata.
`RELEASE_DATE` is the actual calendar date on which the Zenodo record is
published. It is distinct from the preparation date retained in the Major
Proof Index as document provenance.
It also requires the declared URM reconciliation result, clean-reproduction
record, L02 canonical-selection record, and release notes. The release includes
the root licensing notice and both complete license texts. Zenodo must list both
licenses for the mixed-license upload.

Use `release_values.example.json` only as a schema example. Do not publish an
archive containing its placeholders.

The full Git repository remains the chronological development-history and
provenance reference. This capsule is the canonical versioned source for the
released code and proof-reproduction surface.

The Zenodo record is titled **Universal Network Theory** and classified as
**Publication → Preprint**. The title “Universal Network Theory Major Proof
Index” belongs to the internal navigation document, not to the theory itself.
That PDF is the primary publication file and default preview; the deterministic
ZIP is the canonical released code and proof-reproduction source. Its internal
`SHA256SUMS` and `CAPSULE_BUILD.json` records provide the capsule integrity
manifest, so no separate checksum file is uploaded to Zenodo.
The Zenodo description is the Major Proof Index's substantive opening, beginning
with its first UNT paragraph and ending after the Execution Architecture section,
immediately before **Terminology and notation**. Its words remain identical; the
two link labels and destinations are pinned separately in the manual-deposit
checklist so they can be entered with Zenodo's rich-text link control and checked
in the record preview. The extracted-capsule verifier requires the description
and both link targets to remain synchronized with the index.

### Link and custody boundary

The capsule-local links in this README and in the Major Proof Index are the
primary navigation surface and are checked by the extracted-capsule verifier.
Dated, sealed, hash-pinned, and dependency-closure artifacts are preserved
byte-for-byte. Their original repository-relative citations are therefore not
rewritten merely to match the capsule layout. If one of those preserved
citations has no capsule-local target, read it as a **Git-history** reference at
the originating commit and tag recorded in the release metadata. This custody
rule preserves the evidentiary hash; it does not promote an omitted dependency
into a capsule-local reproduction claim.

In particular, the full T-51/C-93 design, executable, `22/22` output, and
critique lane remains a historical Git reference at the recorded originating
identity. Its original
private-path-bearing research envelope is intentionally not copied into the
compact public packet; this location rule does not change the C-93 result.

> **Historical note.** An attempted L14 extension ended before an authenticated
> result and contributes no evidence or premise to the capsule's L4--L12 proof.
> Its disposition remains in the chronological Git record rather than the
> primary proof path.

The current repository-wide status record is included as
`urm/URM_VALIDATION_CURRENT_2026-09-16.md`. It reports `176/176` T-54 checks
and every current scientific chain passing, but the aggregate command exits
`1` because two absent historical auxiliary fixture families have declared
zero scientific/proof weight. The capsule therefore does not claim a fully
green replay of the entire historical URM corpus.
