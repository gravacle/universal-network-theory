# Finite marked-source response statement

## Exact finite structure

For each even `L`, let the declared connected component have the owner-once
edge set already frozen by Gate A-R. The subgroup of the source-preserving
prism group that fixes source site zero has order two. Its exact word-orbit
dimensions at `L=6,8,10,12,14` are respectively

```text
2,176; 33,280; 526,336; 8,396,800; 134,250,496.
```

The quotient uses normalized orbit sums. For a representative word `w` in
orbit `a` and an active owner edge whose swap lands in orbit `b`, the stored
Hamiltonian coefficient is

```text
-sqrt(|a|/|b|).
```

Every physical edge is reconstructed exactly once from its signed orbit.
Every site occupation is reconstructed by averaging the representative word
over the order-two source stabilizer. These are exact finite identities for
the conditional support; the time evolution and current quadrature remain
numerical.

## Authenticated intervention and differential ledger

On the terms-off write slice, the already audited insertion

```text
r0 = 14441248/6075,  Phi = pi/4,  W_R = 1/2
```

maps the blank source to `(B-iX)/sqrt(2)` and obeys exactly

```text
Delta Q_source + sum edge flux - W_R = 1/2 + 0 - 1/2 = 0.
```

After the writer is off and transport is restored, the perturbed state may
carry an instantaneous current; this does not alter the terms-off write
ledger. The evolved finite differential record satisfies

```text
Delta q_after + B Delta J - (1/2)e_source = r_num.
```

Incidence columns telescope exactly. The observed `r_num` remains a raw,
unassigned numerical remainder and is not called a defect.

## Empirical finite result

The refined L6/L8/L10/L12 histories pass `35/35`, `35/35`, `29/29`, and
`29/29`; their compiled target passes `23/23`. Complete direct full-space L6
and L8 occupation/current parity is at approximately `6e-15` or better. The
full differential-ledger L1 remainders are approximately

```text
1.64e-12, 1.62e-12, 1.66e-12, 2.64e-11.
```

All four terminal global responses retain one half-record within the stated
numerical controls.

Across the available finite rows, the response is nonuniform in edge and
shell magnitude and signed-edge currents are oscillatory. Aggregate shell L1
is largest at graph radius two. The farthest-shell L1 sequence is

```text
0.1453025567, 0.04829256663, 0.005579359094, 0.0005258194764.
```

This supports only the empirical description of graph-local concentration
over L6--L12, with decreasing far-shell periodic-return contamination. It is
not a proof of locality, scaling, a limit, or asymptotic behavior.

## L14 boundary and claim classes

The current unaggregated marked-source representation has a conservative
L14 payload upper bound of `94.89795542508364 GiB`, exceeding its `40 GiB`
process guard. It is rejected before allocation. L14 response remains open
and would require a different independently validated representation.

**Proved:** authenticated terms-off write ledger; exact finite stabilizer,
orbit count, signed edge reconstruction, owner-incidence telescoping, and
radial partition arithmetic.

**Adopted:** F3-MDC `alpha=r0` and the canonical source label.

**Conditional:** connected support, `kappa=pi/2`, content, routing, complete
read, and the numerical representation.

**Empirical:** the refined finite L6/L8/L10/L12 response records and their
available-window profile description.

**Open:** independent hostile audit of this packet, any L14 response, physical
distance and physical boundary interpretation, any locality/scaling law, and
Gate A-P.

No grid, continuum behavior, Ward identity, phase, graviton, gravity, `C_R`,
or `G` is inserted or inferred.
