# Pre-output hostile method: Authenticated Accumulation Screen V001

**Status:** `FROZEN_BEFORE_TARGET_HISTORY_OUTPUT`

## Independence boundary

The blind history implementation may read the frozen protocol and pinned
antecedents.  It may not import the target implementation, reuse its edge
actions, or inspect target raw output before its own L4/L6/L8 rows are frozen.

The target uses active-word actions, order-ten exponential stepping, and
Simpson current integration.  The blind implementation constructs disjoint
occupied-to-blank word pairs, advances the Schrödinger equation with RK4, and
uses independently evaluated Simpson currents.  It uses 256/512 steps rather
than the target's 128/256 steps.

## Pre-target gates

Before either implementation runs a target history, verify:

1. the protocol and both implementations are hash-frozen;
2. every prism edge has one owner and every incidence column sums to zero;
3. the fixed writer factor makes the displayed F3-MDC generator a local
   retained-site rotation without dropping an entangled factor;
4. the first blank-target pulse gives `W_1=1/2` exactly;
5. target and blind one-step actions agree on every basis vector through L6;
6. zero-time currents vanish on real word states;
7. both numerical methods preserve a separately declared exact two-site
   pulse-transfer control within their frozen tolerances; and
8. no L4/L6/L8 history raw exists when the pre-output freeze is written.

## Post-output hostile comparison

The comparison must check every event at every seed size.  Relative agreement
limits are `2e-7` for `W_n`, `rho_bar`, sector weights, and normalized capacity,
and `2e-6` for connector differential L1.  Both implementations must agree
exactly on depletion-crossing existence, crossing indices, number-sector
boundaries, common interval, classification, and next gate.

All target and blind event ledgers, norm controls, source factorization,
negative-write guards, capacity denominators, and sector masses are checked
independently.  Any missing row, nonfinite field, hash mismatch, or claim above
the frozen finite-history scope fails closed.

Only a hostile-audited `SEED_ACCUMULATION_SECTOR_L4_L8` may authorize L10/L12.
No seed result establishes a critical density, continuum, metric, emergence,
or gravity.
