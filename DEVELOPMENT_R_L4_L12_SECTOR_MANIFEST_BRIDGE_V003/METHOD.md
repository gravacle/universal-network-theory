# V003 L4--L12 sector-manifest bridge

This narrow bridge reuses the frozen V001 sector-manifest reconstruction for
all event, probability, 99% interval, rational-envelope, atom-partition, and
Stage-2 lineage checks.  L4--L10 inputs and audits remain the exact frozen
canonical inputs.  Only the L12 source paths and L12 audit validator are
adapted to the V003 numerical-repair histories and exact Stage-4 result.

The Stage-4 result must bind both history hashes, report no failures, pass all
checks, retain the original `1e-8` cross-history and terminal-amplitude bound,
and carry twelve authenticated terminal-shard comparisons.  No numerical or
physics definition is changed.  The emitted manifest keeps the frozen
`AUTHENTICATED_RELATIONAL_SECTOR_MANIFEST_V001` schema consumed by Stage 6.
