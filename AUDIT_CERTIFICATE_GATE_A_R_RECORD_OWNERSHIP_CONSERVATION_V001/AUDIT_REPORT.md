# Independent hostile audit of the Gate A-R certificate

## Disposition

`PASS_AFTER_REPAIR`. The certificate's closure premises pass at conditional
record scope, and the authoritative continuation now consistently separates
authorized B-R from open A-P and blocked B-P/M-series promotion.

## Premise reconstruction

- The single-history audit has exact balance `1/2+0-1/2=0`.
- The active-seam hostile audit passes after repair with transported current
  `1/2`, zero endpoint/global residuals, and conditional attachment.
- The stationary G4 hostile audit is sector-qualified: 64 cells, 192
  owner-once carrier supports, and zero carrier-sector residual.
- The join target passes `28/28`. The distinct hostile reconstruction passes
  `18/18` with `PASS_AFTER_REPAIR`. These counts test different layers
  and are not expected to match; the certificate records target `28/28` while
  directly checking hostile disposition and count `18`.
- The join remains absent. GD recoil is optional. Neither is a debt of the
  explicitly selected record parent because those degrees of freedom and
  cross-family exchanges are absent there. If later selected, they acquire
  owner-once obligations in that enlarged parent.
- Every dependent write retains adopted
  `alpha=r0=14441248/6075`, not a bare-F3 derivation.

The certificate therefore supports
`GATE_A_R_CLOSED__CONDITIONAL_F3_MDC_RECORD_SCOPE` and authorization of R-B
microscopic accumulation. It does not close A-P, M-A, any continuum
diagnostic, Ward identity, or gravity claim.

## Repaired authority chain

`CURRENT_CONTINUATION.md` now calls the global-action gap Gate A-P/M-series,
says explicitly that A-P incompleteness does not block authorized B-R, and
blocks only B-P/M-series macro-response promotion. Its later owner-join text
is likewise scoped to A-P and calls recoil optional; those owners are not B-R
prerequisites unless the selected record parent activates them. The stale
unqualified phrases “Plan-level Gate A remains open” and “do not launch Gate
B” are absent.

The ledger and V002 schedule are also internally consistent: R-B does
not imply A-P, M-series, continuum behavior, Ward structure, or gravity.
