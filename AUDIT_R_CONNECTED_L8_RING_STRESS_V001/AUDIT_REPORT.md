# Independent hostile audit

## Disposition

`PASS_CONTROLLED_NUMERICAL_PREPARED_STRESS`.

An independently assembled 256-state occupation-basis Hamiltonian is exactly
Hermitian and commutes exactly with total occupation. Direct operator
commutators confirm the target convention
`dot(q_i)=-J_i+J_(i-1)`.

Independent diagonalization and energy-basis current integration reproduce
the alternating occupations `0.312588864610359/0.687411135389641`, currents
`+/-0.093705567694821`, and nearest-neighbor connected correlation
`-0.026133160509504`. Integrated currents close every site ledger with L1 and
Linf residuals far below `2e-11` and `3e-12`. Norm, energy, total occupation,
and the full number-sector distribution provide independent controls.

Four odd first-coordinate values times eight third-coordinate values give 32
periodic y-rings. They cover all 256 prepared heads once. Aggregation gives
retained total 128 within tolerance, absolute oriented throughput
`23.988625329874075`, and signed ring-current sum consistent with zero.

The target script runs with `PYTHONWARNINGS=error` and emits no warning. Its
current-operator transform uses two explicit contraction steps, avoiding the
prior problematic contraction. Hard-coded RESULT values agree with the fresh
target output and independent reconstruction within their displayed precision
and preregistered tolerances.

The attachment is explicitly inherited as adopted `alpha=r0`, not bare-F3
derived. The complete failure-inclusive qutrit/controller/work/reference
product read remains in force.

This is controlled floating-point evidence for one prepared pulse on 32
identical connected rings. It is not an exact theorem, generic retention,
critical accumulation, a continuum limit, Ward behavior, or gravity.

No material defect was found at the declared scope.
