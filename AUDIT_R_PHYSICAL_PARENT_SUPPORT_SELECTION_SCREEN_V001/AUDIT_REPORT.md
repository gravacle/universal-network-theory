# Hostile audit — physical-parent support selection

**PASS_EXACT_NON_SELECTION_BOUNDARY.** Independent L4/L8 constructions reproduce the bipartite layer census, degree two, cycle decomposition, and a distinct right-label-permuted equal-energy competitor. The exchanged labels belong to different cycles while their declared source occupations are identically one half, so source preparation is invariant.

Exactly, `f(d)-f(2)=Ud(d-2)^2+(Delta/2)(d-2)`. The binding lower-degree comparison is `d=1`, hence `0<Delta<2Ud`; all `d>=3` are higher. This favors a degree class, not a labeled word.

For `h>0`, negative one-link flips connect the Boolean hypercube. Finite irreducible stoquastic Perron–Frobenius gives a unique strictly positive ground vector, and permutation symmetry plus uniqueness makes it invariant—not an exact support word. Independent K3,3 diagonalization confirms six classical words, a positive gapped transverse ground state, orbit equality, and broad non-word support. BS09, BS10 and BS11 commute with incidence; the BS06 flip does not.

The non-selection boundary is valid under the declared anchoring: right-layer source states are uniform and left sites blank. Any lineage or port map distinguishing the swapped labels is additional conditional selector data requiring complete ownership, not an unchanged-parent consequence. FPSS remains conditional. No correction is required before promotion. No physical grid, defect, continuum, Ward, or gravity claim is promoted.
