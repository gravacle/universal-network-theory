# Hostile audit result

`PASS_AFTER_REPAIR`.

Independent matrix reconstruction confirms the two-stage unitary, the
`a -> b` current orientation, transported seam current `1/2`, exact local
residuals `(0,0)`, and exact global residual `0`.  It also confirms that bare
F3 leaves the port normalization non-identifiable and that `alpha=r0` is only
an adopted conditional attachment.

The repaired A09--A09a supplies branch Hamiltonians, propagators, an initial
density operator, and a complete terminal effect family.  Independent
reconstruction confirms equal-branch CTP normalization `Z=1`.  The claim is
now correctly limited to a finite operator CTP generator, not a continuum
action; the prior material defect is closed.

The global census properly fails before action formation.  Gate A remains open; Gate B is
unauthorized; no Ward or gravity result is promoted.
