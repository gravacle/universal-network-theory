# Autonomous connected L10 accumulation V001

This packet applies the hostile-audited finite-orbit basis to the next member
of the conditional connected-support record. Fifty connected twenty-site
components cover 1,000 sites and own 1,500 internal and connector edges once.
The uniform F3-MDC preparation and one simultaneous BS09 Hamiltonian at
conditional `kappa=pi/2` are unchanged.

Run:

```sh
PYTHONWARNINGS=error python3 -B compute_connected_l10.py
```

The exact finite group compresses the 1,048,576-word component to 105,376
orbit states. Order-10 unitary Taylor evolution compares 1,024-step and
2,048-step Simpson current integrations. The script validates frozen raw
`RESULT.json` within `8e-10` and must finish with
`PASS__R_AUTONOMOUS_CONNECTED_L10_ACCUMULATION__24/24`.

Set `REPORT_RESOURCES=1` to print wall time and maximum RSS without storing
run-dependent values in the canonical result. `RUN_OBSERVATION.md` freezes one
such observation on the declared 48 GiB host.

Only the finite orbit basis is exact. Evolution and current quadrature remain
numerical. Support and mission parameters remain conditional. Residuals are
unassigned record-ledger terms, not defects. No grid, continuum, Ward, phase,
criticality, graviton, or gravity claim is made.
