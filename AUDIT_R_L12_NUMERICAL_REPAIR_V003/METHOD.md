# L12 V003 exact numerical-repair adjudication

This successor admits only the already-authorized V003 capacity extension:
Target Krylov checkpoints through 96 with at most 256 low-memory
subdivisions, and Hostile recurrence checkpoints through degree 128.  The
physical graph, admission and routing times, quadrature rules, observable
definitions, and all convergence and comparison tolerances remain unchanged.

The adjudicator reuses the frozen V001 complete-state checker for every row,
ledger, sector, and terminal-amplitude predicate.  It supplies an authenticated
schema projection for the process-parallel V003 histories, validates the V003
capacity/provenance fields, and changes only the three obsolete method caps and
the terminal roots.  Target terminal state is read from the new full replay.
Hostile sharp events 1--11 and its prefix-11 state are authenticated against
the preserved V002 summary and immutable prefix files; event 12 must be newly
solved and pass the original `8e-11` endpoint/tail predicates.

Publication is owner-once.  A failed comparison produces no normalized blind
history.  This gate claims only finite L12 history and complete H11 terminal
state agreement to `1e-8`; it makes no spectrum, continuum, emergence, or
gravity claim.
