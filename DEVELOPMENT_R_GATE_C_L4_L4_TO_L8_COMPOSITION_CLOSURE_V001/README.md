# L4 + L4 to L8 composition-closure target

Run `PYTHONWARNINGS=error python3 -B compute_closure.py`. The script is self-contained, pins the frozen protocol and lower-order adversarial result, and runs only L4 and L8.

The mandatory disposition is `STOP_EXPONENTIAL_LOWER_BOUND__EXACT_D8_UNRESOLVED`. This is a conditional finite coherent-interface obstruction, not a scaling, grid, continuum, Ward, phase, graviton, or gravity result.
