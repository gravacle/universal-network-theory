# Result

`STOP_EXPONENTIAL_LOWER_BOUND__EXACT_D8_UNRESOLVED`

Owner surgery and full-state reconstruction pass. `D4=16`; the conservative common L8 lower bound is `D8>=251`; exact `D8` remains unresolved in `[251,256]`; `D=256` reconstructs. The equal-time lower-order boundary summary fails closure in the separately pinned 19/19 adversarial result.

This is the mandatory L8 stop. It is not a continuum, scaling, Ward, phase, graviton, or gravity result and remains subject to independent hostile audit.
