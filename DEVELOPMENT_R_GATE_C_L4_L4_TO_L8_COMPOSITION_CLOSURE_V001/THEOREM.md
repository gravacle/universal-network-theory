# Conditional finite composition-closure screen

The frozen six-remove/six-add surgery maps two relabeled periodic L4 owner sets exactly to the canonical 24-edge degree-three L8 support. No other owner changes. The uniform blank-even/`(B+x)/sqrt(2)`-odd source factorizes across the declared blocks; simultaneous source-off evolution uses `H=-sum_e T_e` at conditional `kappa=pi/2`, followed by the complete product read.

The L4 half-cut coefficient matrix is `16 x 16` and has solver-stable numerical rank 16. The L8 `A|B` matrix is `256 x 256`. The target threshold estimate is 254, but the separate hostile refinements do not resolve its last modes: the common robust lower bound is only `D8>=251`, while exact reconstruction at `D8=256` is verified by the full SVD. Thus the honest current interval is `251<=D8<=256`, giving `D8/D4>=15.6875` and `D8/256>=0.98046875`. The exact minimum remains unresolved.

This already triggers the frozen exponential-lower-bound stop. No truncation or larger-size machinery is introduced. The pinned 19/19 lower-order adversarial packet proves that equal-time `Q`, four-port density, incident currents, and port correlations do not close future joined evolution.

Exact owner surgery, source factorization, and Schmidt minimality are proved. Evolved spectra, threshold ranks, currents, correlations, and residuals are numerical. The join and parent parameters remain adopted/conditional. No scaling law, grid, continuum, Ward identity, phase, graviton, or gravity follows.
