# Independent hostile audit

## Disposition

`PASS_CONDITIONAL_INTERACTION_STRESS`.

## Pair dynamics

In the ordered one-occupation basis `A=|x,B>`, `B=|B,x>`, direct
multiplication gives

`(H/t)^2 = [[1,-1],[-1,-1]]^2 = 2I`.

At `tau=pi*hbar/(2sqrt(2)t)`, the propagator on this block is
`U=-i(H/t)/sqrt(2)`. It sends `(A+B)/sqrt(2)` to `iB`. The blank-blank and
occupied-occupied sectors have zero transfer and zero onsite difference, so
they remain fixed.

The complete `(BB,Bx,xB,xx)` probabilities therefore change from
`(1/4,1/4,1/4,1/4)` to `(1/4,1/2,0,1/4)`. Their TV distance is `1/4`; local
occupations move from `(1/2,1/2)` to `(1/4,3/4)`; and the total-number law
remains `(1/4,1/2,1/4)`.

With the BS07 orientation from site 1 to site 2, normalized one-particle
evolution has `<J>(s)=sin(2sqrt(2)s)/sqrt(2)`. Its pulse integral is `1/2`.
The one-particle sector has full-state weight `1/2`, giving oriented physical
current `1/4`. Thus the site ledgers are exactly
`-1/4+1/4=0` and `+1/4-1/4=0`.

## L8 census and read

The prior prepared history has 256 odd-first-coordinate heads. Pairing even
to odd second coordinates gives 128 disjoint blocks and covers all heads
once. Multiplication of the pair results yields retained total 128,
module-oriented absolute throughput 32, and expected positional occupations
32/96. The net closed-family boundary flux remains zero.

The terminal product includes complete qutrit PVMs and inherited controller
`OK/FAILURE`, work, and reference PVMs. Retaining the zero-probability failure
effect keeps the declared read complete; no outcome is conditioned away.

## Scope ceiling

The result is exact only for the conditional prepared tensor product of 128
disjoint interaction blocks. Because equal-content occupations become
indistinguishable after coupling, it does not establish motion or persistence
of an individual source-lineage label. It does not establish generic
interaction stability, criticality, a phase, a universal accumulation law,
continuum behavior, Ward structure, or gravity.

No material defect was found at this scope.
