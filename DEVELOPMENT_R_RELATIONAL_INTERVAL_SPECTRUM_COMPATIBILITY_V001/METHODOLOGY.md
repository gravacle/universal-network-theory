# Stage 6 blind compatibility successor V001

**State:** `CANDIDATE_PRE_OUTPUT__NO_PHYSICAL_SPECTRUM_EXECUTED`

This bounded successor repairs three interface contradictions without changing
the finite prism Hamiltonian, response channel, Krylov recurrence, numerical
guards, fit families, or pass thresholds frozen by the public relational
interval protocol.

The sector-plan producer accepts only a separately hash-pinned authenticated
L4--L12 accumulation manifest.  It reauthenticates all target, blind, and
hostile-audit files, reconstructs the complete rational atom partition and
every `q_by_L` value, and emits exactly the distinct positive L10/L12 sectors.
The plan is created owner-once as a single-link, read-only file.

The runnable blind freeze uses the exact schema consumed by the target
adjudicator: `RELATIONAL_INTERVAL_BLIND_METHOD_FREEZE_V001`.  It binds the
manifest, deterministic plan, this source freeze, independent worker, and
index publisher.  It carries the literal nested sector pairs and the explicit
false declaration `target_code_or_matrices_imported` before any target
interval spectrum may run.

Each planned row receives an owner-once execution credential after the blind
method, manifest, exact sector plan, worker, publisher, control, and common
contract bytes have been authenticated.  The credential binds one sector and
one future physical output path.  The worker refuses before numerical work if
that credential is missing or stale and embeds the validated authorization in
the owner-once row.

The worker wraps the already-audited independent centerline construction at
its frozen byte hash.  It broadens only the row-policy domain from the former
two centerline rows to all positive half-sector charges at L10/L12.  It
continues to construct the graph, fixed-charge basis, translation orbits,
sparse blocks, and two-pass fully reorthogonalized Krylov chains without a
target import or matrix.  Its serializer exposes the already-adopted public
definitions

```text
Delta_act = lowest positive pole above the authenticated weight floor
chi_tau   = sqrt[sum_a w_a/omega_a^2 / sum_a w_a]
R_low     = w_(Delta_act)/sum_a w_a
```

and the fixed residual, Hermiticity, orthogonality, checkpoint, 128-vector,
2000-matvec, three-hour, and six-GiB guards.  No physical row is generated by
this preparation packet.

The index publisher accepts only owner-once resolved rows for the exact frozen
sector set and revalidates each row's execution credential.  It stores rows and
credentials as repository-relative path/hash bindings keyed
`L:q`, uses the target-adjudicator field names `manifest_sha256` and
`method_freeze_*`, and independently reconstructs every atom classification
from the public fit definitions.  It imports neither the target classifier
nor any target numerical implementation.

The bounded adjudication entrypoint revalidates the complete row/credential
census, unique nonces, exact frozen worker authority, and physical-output path
bindings before invoking the already-frozen target adjudicator.  A substituted
authority, credential, row, or row path fails before adjudication.

All source files are frozen before any runnable manifest-specific freeze.
All plan, freeze, row, and index publication is no-clobber owner-once.  A
missing, stale, aliased, reordered, noncanonical, or already-existing input or
output fails closed.

This packet certifies interface compatibility only.  Histories, spectra,
scaling, continuum behavior, algebraic closure, emergence, and gravity remain
conditional or open according to their existing claim classes.  No grid,
graviton, Ward axiom, reservoir, or continuum premise is introduced.
