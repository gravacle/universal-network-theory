# Hostile V004R2 immutable storage-cache successor

**Status:** `FROZEN_PRE_CACHE_PRE_HISTORY`; successor to the preserved but
independently failed V004 preflight freeze, not an amendment of those bytes.

## Scope and claim boundary

V004R2 changes only how exact indices already reconstructed by hostile V003
are stored and authenticated. It does not change the reversed-combination
carrier basis, full canonical lineage masks, hostile edge order, exchange
coefficient `-1`, current orientation, ALLOW/REQUIRE behavior, owner-once
admission, terminal-child streaming, rough/sharp evolution, quadrature,
tolerances, ledger, or observable. It introduces no grid, graviton, Ward
axiom, reservoir, continuum premise, or gravity claim.

The target-V004 and hostile-V003 six-hour misses are retained as two distinct
resource obstructions, each with an independently hashed obstruction record,
execution log, and wall monitor. All six paths are distinct. Both records say
`resolved=false`, `physical_output_created=false`, `wall_limit_seconds=21600`,
and `wall_seconds=21627`; their implementation, method, and freeze identities
are individually pinned. The 48/48 structural workspace observation and the
cross diagnostic are evidence only. The latter's q11 difference
`4.826848e-5` forbids restart or equivalence use of either incomplete rough
workspace. V004R2 permits only a full cached rerun and never reads or mutates
the preserved workspaces.

## Exact immutable cache

For every `q=0..L`, the cache stores the full reversed-combination words,
`3L+1` edge offsets, and concatenated source/target ranks for every hostile
edge. For every `(event,q)` in the complete admission triangle it stores the
occupied/new and old ranks. For every nonterminal `(event,q)` in the complete
lineage triangle it stores stay/write row ranks. There are exactly
`2(L+1)^2` array files and one manifest: at L12, 338 arrays and 339 files
including the manifest.

The consumer rejects omissions, extras, duplicates, reordered records,
unexpected keys, wrong paths, dtypes, shapes, byte counts, hashes, edge order,
sector censuses, or totals. It then authenticates exact basis order and
Hamming sectors, offsets and per-edge counts, source predicates, target XOR
identities, admission add-bit identities, and full lineage ranks.

Cache directories are canonical packet-local paths and become mode `0555`;
members and the manifest are mode `0444`. The consumer opens the directory
with `O_DIRECTORY|O_NOFOLLOW`, requires the exact member census, opens each
ordinary non-writable member once with `O_NOFOLLOW`, hashes through that held
descriptor, and maps only duplicates of the same authenticated descriptor.
It never reopens a member by path, closing the hash-then-reopen replacement
gap.

## Exact L12 resource certificate

The raw cache excluding offsets is `826,218,064 B`; offsets are `3,848 B`;
actual cache arrays are `826,221,912 B`. The maximum two-prefix state is
`8,773,664,640 B`, so state plus actual cache is `9,599,886,552 B`, and with
the 1-MiB filesystem reserve is `9,600,935,128 B`. The maximum q12 cache plus
terminal admission mapping is `224,797,960 B`; including the needed q11 prior
word basis gives a conservative terminal mapping of `234,782,536 B`. The
frozen authentication peak certificate is `252,944,080 B`.

The independent guards remain: `1,400,000,000 B` numerical workset,
`536,870,912 B` I/O window, 20-GiB logical scratch, 16-GiB RSS, and 21,600 s
wall time. The clock begins before freeze, gate, full-file hash, and semantic
cache authentication. L12 free space is tested on the canonical workspace
filesystem using the exact state/cache/offset/reserve arithmetic. These are
analytical certificates, not empirical RSS or runtime results.

## Dependent gate chain

All six future gates are absent at freeze.

1. `DUAL_SIX_HOUR_OBSTRUCTION_GATE_V004R2.json` must bind the exact two
   obstruction records, their nested evidence, frozen successor files, and
   the preserved-workspace non-use disposition before any cache directory can
   be created.
2. `CONTROL_EXECUTION_GATE_V004R2.json` must bind the actual immutable cache
   manifest hashes and canonical output/workspace paths for L4/L6/L8.
3. `CONTROL_GATE_V004R2.json` may pass only by binding the actual three output
   hashes, their cache manifests, and an actual all-pass comparison report.
4. `L10_EXECUTION_GATE_V004R2.json` binds the passing control gate and the L10
   cache/output/workspace. `L10_GATE_V004R2.json` then binds the actual L10
   output and comparison report.
5. `L12_EXECUTION_GATE_V004R2.json` binds the entire preceding chain, the
   actual L12 cache/output/workspace, and the existing target V004 prerequisite
   gate (`273/273`, SHA-256 pinned).

Any missing/mutated prerequisite refuses before physical workspace creation.
A numerical, scratch, RSS, or wall miss refuses without writing a physical
result file; its canonical workspace remains available for separate custody.
Only a fully resolved, in-budget execution can write the immutable result.

## Classification

The current freeze and preflight can prove only nonphysical index identity,
resource arithmetic, custody, refusal behavior, and a future executable
protocol. Cache payloads and histories remain absent. Any later history is a
finite empirical record until an independent hostile adjudication passes.
Spectrum, `z=1`, criticality, anomaly closure, continuum behavior, emergence,
and gravity remain open.
