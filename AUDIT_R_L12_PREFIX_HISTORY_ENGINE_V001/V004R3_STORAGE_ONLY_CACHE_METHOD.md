# Hostile V004R3 strict staged-result successor

**Status:** `FROZEN_PRE_CACHE_PRE_HISTORY` after the independent V004R2 gate
validator failure. Every V004R2 byte is preserved and hash-bound as evidence.

## Repair boundary

V004R2 correctly froze the dual-obstruction custody, cache census, exact index
semantics, canonical paths, stable-descriptor consumer, resource arithmetic,
and hard locks. It is nevertheless superseded because its staged history
binding accepted a synthetic object containing only `schema`, `L`, cache hash,
`comparison.resolved`, and `resource.passed`. Thus its control chain did not
authenticate a complete physical result.

V004R3 changes only that control plane and the builder's stable write/hash
custody. Carrier basis order, canonical lineage masks, hostile graph and edge
order, coefficient `-1`, signed current orientation, ALLOW/REQUIRE behavior,
owner-once admission, evolution, tolerances, ledgers, and observables remain
unchanged. No grid, graviton, Ward axiom, continuum assumption, or gravity
claim is introduced.

## Complete result contract

Every staged history must have the exact V004R3 top-level census and bind:

- its frozen consumer, method, and freeze hashes;
- the canonical cache path and exact cache-manifest hash;
- the exact prerequisite-gate hash dictionary for its stage;
- all `L` event rows with exact row and solver schemas, event/prefix sequence,
  combinatorial dimensions, finite observables, `L+1` sector weights, solver
  convergence, algorithm identity, and allocation bounds;
- the complete resolved comparison schema, finite rough/sharp and ledger
  residuals, and all `L` response sequences;
- all `L` terminal shards with exact q/path/shape/logical-byte identities; and
- the complete resource schema with the frozen scratch, workset,
  authentication, RSS, and wall limits plus in-bound measured values.

Partial, extra-key, wrong-path, wrong-lineage, nonfinite, unresolved, or
resource-incomplete documents refuse. Physical output is created only after
the same validator accepts the in-memory completed result.

## Positive comparison contract and stage chain

Control and L10 comparison reports have an exact schema. They bind the exact
history rows used by their pass gates, require `resolved=true`, require a
strictly positive test count, require `checks_passed == checks_total`, and
require finite observable and sector differences no larger than the positive
declared tolerance. Consequently a `0/0` report cannot authorize any stage.

The dependent sequence is exact:

1. The dual obstruction gate binds the two current-run obstruction records,
   all four nested log/monitor records, six-path and six-hash distinctness,
   and the frozen V004R3 files.
2. The L4/L6/L8 execution gate binds all three actual cache-manifest hashes and
   all canonical output/workspace paths.
3. The control pass gate binds all three complete output hashes, their
   cache-manifest hashes, the actual positive comparison report, and both
   prerequisite hashes.
4. The L10 execution and pass gates bind that control gate, the complete L10
   output, its cache, its positive comparison, and the precise preceding gate
   hashes.
5. The L12 execution gate binds the control and L10 pass hashes, the L12 cache
   and canonical paths, and the original target V004 prerequisite including
   its exact hash and `273/273` result. A separate aggregate launch gate also
   requires both target and hostile L10 passes before contemporaneous launch.

Every gate has an exact key census, identity, classification, claim boundary,
frozen hash set, canonical paths, and prerequisite dictionary. The nonphysical
preflight executes each validator on a complete minimal synthetic record and
then proves rejection under field omission, lineage mutation, partial result,
resource mutation, path mutation, comparison mismatch, `0/0`, and prerequisite
mutation. No synthetic object is written as a physical history.

## Storage and resource custody

The V004R2 cache arithmetic remains exact, including L12 raw indices
`826,218,064 B`, offsets `3,848 B`, actual arrays `826,221,912 B`, maximum
two-prefix state `8,773,664,640 B`, combined bytes `9,599,886,552 B`, and
1-MiB-reserved requirement `9,600,935,128 B`. The q12/admission mapping,
terminal mapping, and authentication peak remain `224,797,960 B`,
`234,782,536 B`, and `252,944,080 B` respectively.

The builder hashes each newly written member through the still-open descriptor,
then verifies path identity before sealing its mode. The consumer retains the
V004R2 one-open, no-follow, ordinary-file, mode-0444 member authentication and
mode-0555 directory census. The six-hour clock begins before all gate and cache
authentication. A numerical or resource failure cannot publish a result.

Parallel L12 execution retains the 16-GiB RSS guard independently for each
process. Its frozen conservative certificate is `34,865,626,528 B` combined
RSS plus mappings against a `48,000,000,000 B` nominal host, leaving
`13,134,373,472 B` (`12.2323 GiB`). The target and hostile disk minima are
`9,600,951,508 B` and `9,600,935,128 B`, totaling `19,201,886,636 B`; the
observed-free launch floor is `44,735,000,000 B`. The aggregate gate pins a
canonical telemetry output with launch/completion epochs, per-role peak RSS
and mapped bytes, disk samples, wall times, exit codes, and output hashes.
These are scheduling/resource claims only.

## Current classification

V004R3 is presently a frozen nonphysical protocol only. All six authorization
gates, caches, workspaces, and histories are absent. A passing preflight proves
only schema/refusal/index/resource control. Independent hostile re-audit is
mandatory before activation. Finite history, scaling, spectrum, criticality,
continuum behavior, emergence, and gravity remain open.
