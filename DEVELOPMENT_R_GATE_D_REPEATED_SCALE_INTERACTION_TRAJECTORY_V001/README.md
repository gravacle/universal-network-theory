# R-D repeated-scale interaction trajectory

This packet repeats the audited native interaction/retention block at `L=4`
and `L=8` under a common prepared density. It reports raw record quantities
only; no continuum form is assumed.

Run:

```text
python3 -B DEVELOPMENT_R_GATE_D_REPEATED_SCALE_INTERACTION_TRAJECTORY_V001/verify_repeated_scale_trajectory.py
```
