# Hostile audit — connected L8 circuit order screen V001

## Disposition

**PASS_CONTROLLED_NUMERICAL_ORDER_SCREEN.** A fresh 65,536-state reconstruction imports neither target code nor target data. It applies forward, exact reverse, and interleaved schedules at depth 3. Each schedule uses the same 24 unique physical supports exactly once per depth: 72 events per schedule and 216 independently reconstructed events total. All 24 cumulative currents, including all eight rung currents, are active in every schedule. Gate endpoint signs and owner ledgers pass.

All three pairwise comparisons were made on the complete terminal probability vector, full state overlap, and 16-component occupation vector. The reconstructed total variation, fidelity, and occupation-L1 values agree with the target. The forward schedule also agrees with the separately audited depth-3 trajectory, including terminal occupations, traffic, correlations, retention, and raw controls.

## Narrow conclusion and custody

The result establishes only that schedule is necessary selected-parent data for this noncommuting finite circuit: the support census alone does not fix the terminal record distribution. It derives no preferred order, probability measure over orders, averaging rule, or selection law.

The terminal read is the complete failure-inclusive product PVM and attachment remains inherited adopted `alpha=r0`, not bare-F3 derived. Tuple labels are finite enumeration, not a physical grid. Raw residuals remain unassigned rather than physical defects or exact zeros. No simultaneous-Hamiltonian, continuum, critical/scaling, Ward, macroscopic-dynamics, or gravity claim is promoted. No material defect or repair was found.
