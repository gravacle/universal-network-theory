# Global stationary carrier/seam sector on the inherited G4 family

## 1. Scope and claim classes

- **Proved:** owner-once native carrier action on every inherited `G_4`
  nearest-neighbor edge; exact operator continuity on every region; exact
  periodic telescoping; existence of a stationary finite Gibbs state; exact
  finite CTP normalization under a complete terminal read.
- **Adopted:** the F3-MDC working-law context and the already-declared `G_4`
  periodic family.
- **Conditional:** common finite `t`, inverse temperature `beta`, and the
  declared carrier-sector preparation/read.
- **Open:** all non-carrier global owners, the physical quotient, descent and
  Ward residuals, Gate B, and gravity.

No new grid, distance, graviton, or Ward axiom is introduced. Coordinates
below enumerate the inherited periodic family and its six already-declared
oriented neighbors; they are not promoted to physical spatial coordinates.

## 2. Owner-once edge action

Let `V=(Z/4Z)^3`. For each `u in V` and positive generator
`mu in {1,2,3}`, include exactly one edge

\[
 e=(u,u+\hat\mu).
\tag{S01}
\]

This canonical orientation gives `3*4^3=192` undirected physical supports,
each once. On each edge use the native BS07--BS09 qutrit transfer

\[
 T_e=\sum_{x=0}^1(|B,x\rangle\langle x,B|
                  +|x,B\rangle\langle B,x|),
\quad
 J_e=i\sum_{x=0}^1(|B,x\rangle\langle x,B|
                  -|x,B\rangle\langle B,x|),
\tag{S02}
\]

and define

\[
 H_{\rm car}=-\sum_{e\in E}t_eT_e+\sum_{v\in V}\epsilon_vq_v,
\qquad t_e\in\mathbb R.
\tag{S03}
\]

Each link support and coefficient occurs once. `J_e` is derived by
commutation and is not a second action term.

## 3. Exact global continuity

Let the incidence matrix use `B_(v,e)=-1` at the tail and `+1` at the head.
Direct commutation with (S03) gives

\[
 \dot q_v=\sum_e B_{ve}{t_e\over\hbar}J_e.
\tag{S04}
\]

The onsite term commutes with every `q_v`. For any region `R subset V`,
internal edge columns cancel and

\[
 {dQ_R\over dt}
 =-\sum_{e\in\partial^+R}{t_e\over\hbar}J_e
  +\sum_{e\in\partial^-R}{t_e\over\hbar}J_e.
\tag{S05}
\]

For `R=V`, every column of `B` sums to zero, proving
`d sum_v q_v/dt=0`. This is physical Hamiltonian transport conservation,
not an algebraic seam convention. Across any bipartition the same edge is
outward for one side and inward for the other, so it telescopes exactly once.

The previously audited two-cell pulse is a nonzero solution of (S04)--(S05):
one edge has integrated current `1/2`; it appears with opposite signs in the
two cell ledgers.

## 4. Stationary state and complete read

For finite real `beta>=0`,

\[
 \rho_\beta={e^{-\beta H_{\rm car}}\over
                   {\rm Tr}(e^{-\beta H_{\rm car}})}
\tag{S06}
\]

exists on the finite qutrit product space and satisfies
`[rho_beta,H_car]=0`. This proves stationarity; it does not prove that a
laboratory or the full F3 parent dynamically selects this state. The complete
terminal instrument is the product basis read

\[
 E_{\mathbf y}=|\mathbf y\rangle\langle\mathbf y|,
 \quad \mathbf y\in\{B,0,1\}^{64},\quad
 \sum_{\mathbf y}E_{\mathbf y}=I.
\tag{S07}
\]

For branch propagators generated by (S03),

\[
 Z_{\mathbf y}[+,-]={\rm Tr}
 (E_{\mathbf y}U_+\rho_\beta U_-^\dagger),
\qquad Z=\sum_{\mathbf y}Z_{\mathbf y}.
\tag{S08}
\]

At equal branches `Z=1`. Equations (S03), (S06)--(S08) therefore give a
complete exact finite CTP generator for the stationary carrier sector.

Time-reversal invariance of the real Hamiltonian makes the equilibrium mean
current zero. This does not erase the current operator or the audited
source-driven nonzero history; it prevents an equilibrium circulation claim.

## 5. Owner promotion and residual

Within the carrier sector only, the following global rows are now explicit:

| GK02 row | sector disposition |
|---|---|
| connector | `EXACT_GLOBAL_G4_CARRIER_SECTOR` |
| seam/period | `EXACT_GLOBAL_OWNER_ONCE_TELESCOPING` |
| state | `EXACT_STATIONARY_GIBBS_MEMBER__SELECTION_OPEN` |
| measure | `EXACT_COMPLETE_PRODUCT_READ` |

The discrete global carrier residual is exactly zero. The full global action
is nevertheless `OWNER_INCOMPLETE`: support/shared-midpoint dynamics, recoil,
Maxwell, constraint/projector, matching beyond this carrier sector, retained
field, general boundary apparatus, self/contact completion, and the lawful
connected/Legendre/Schur quotient are not supplied by (S03).

Hence the only lawful full-parent outputs remain

`PHYSICAL_DESCENT_RESIDUAL=UNDEFINED` and
`PHYSICAL_WARD_RESIDUAL=UNDEFINED`.

Plan-level Gate A remains open and Gate B remains unauthorized.
